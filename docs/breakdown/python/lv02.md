# 制御構造

**製品**: Python ／ **本ページ件数**: 102 件 ／ **出典区分**: Python 公式ドキュメント出典

???+ note "if 文 （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 165 | if 文の基本 | if 条件: ブロック。条件は bool(obj) で評価される。論理的 falsy 値はすべて偽として扱われる。 | PYDOC |
    | 166 | if-else | 条件が偽のとき else ブロックを実行。else: の後にコロンと改行、インデントが必要。 | PYDOC |
    | 167 | elif による多分岐 | elif は else if の短縮形。条件式を順次評価し最初に真となった節だけ実行する。 | PYDOC |
    | 168 | 条件式 (三項演算子) a if cond else b | 代入や引数で値が必要な場面で 1 行 if-else。式として評価される。 | PYDOC |
    | 169 | ネストした if と早期 return | PEP 8 風には深いネストを避け guard clause (条件不一致なら return) を多用する。 | PYDOC |
    | 170 | if での None 判定 is None | None との比較は == ではなく is を使う。__eq__ をオーバーライドした型で == None が想定外結果になる事故を避ける。 | PYDOC |
    | 171 | if での bool 直接判定 | if x == True は冗長で if x: が標準。逆に厳密な True/False 判定が必要なら is True を使う。 | PYDOC |
    | 172 | if の評価順と短絡 | and/or は左から評価し結果が確定した時点で評価を停止する。副作用のある関数呼び出しの順序に注意。 | PYDOC |

??? note "for 文 （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 173 | for var in iterable: の基本 | iterable から要素を順次取り出し var に束縛してブロックを実行する。range, list, dict, ファイル, ジェネレータなどに使える。 | PYDOC |
    | 174 | for-else 節 | ループが break で抜けずに正常終了したときだけ else ブロックを実行する。検索ループの「見つからなかった処理」に便利。 | PYDOC |
    | 175 | for と enumerate() | for i, x in enumerate(it, start=1): のように index と要素を同時に取得。range(len(...)) より読みやすい。 | PYDOC |
    | 176 | for と zip() | for a, b in zip(xs, ys): で並列走査。strict=True (3.10+) で長さ不一致を ValueError にできる。 | PYDOC |
    | 177 | for とアンパック for a, b in pairs: | iterable の各要素がさらに iterable なら直接アンパックして受けられる。 | PYDOC |
    | 178 | for と dict 走査 | for k in d: は key を、for k, v in d.items(): は (key, value) を返す。 | PYDOC |
    | 179 | for とファイル走査 | with open(...) as f: for line in f: で 1 行ずつ遅延読み込み。大規模ファイルでもメモリ効率が良い。 | PYDOC |
    | 180 | for と reversed() / sorted() | 走査順を制御する慣用句。元の iterable を変更しない。 | PYDOC |

??? note "while 文 （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 181 | while 条件: の基本 | 条件が真である限りループ。条件評価でステートが進む構造に。 | PYDOC |
    | 182 | while True と break | 無限ループ + 内部の break が if-elif より読みやすい場面で多用される。 | PYDOC |
    | 183 | while-else 節 | for-else と同じく break せず終了したときに else を実行する。 | PYDOC |
    | 184 | while と セイウチ演算子 | while (line := f.readline()): のように代入と判定を同時に行えるイディオム。 | PYDOC |

??? note "ループ制御 （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 185 | break | 最内側のループを脱出する。ネストしたループ全部を抜けたい場合は別関数化 + return が定石。 | PYDOC |
    | 186 | continue | ループ本体の残りをスキップして次のイテレーションへ。多用すると可読性が落ちる。 | PYDOC |
    | 187 | pass | 何もしない文。空ブロックの placeholder として class/if/except 等で使う。 | PYDOC |
    | 188 | ループ終了後の else の使い分け | 「探したけど無かった」を for-else / while-else で表現すると変数フラグ削減につながる。 | PYDOC |

??? note "match 文 （11 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 189 | match-case の概要 (PEP 634) | Python 3.10 で導入された構造的パターンマッチ。switch の上位互換でパターンの分解 + バインドができる。 | PYDOC |
    | 190 | リテラルパターン | case 0: case 'on': などリテラルとの一致。True/False/None は同一性比較、それ以外は等価性。 | PYDOC |
    | 191 | キャプチャパターン case x: | 識別子だけは「任意の値」を捕捉して x にバインドする。最終 default 節の役目で _ を使う場合は捕捉しない。 | PYDOC |
    | 192 | ワイルドカード case _ | 値を捕捉せずに「何でも一致」。最終節としてよく使う。 | PYDOC |
    | 193 | シーケンスパターン case [a, b, *rest] | list/tuple 相当のシーケンス分解。str/bytes/bytearray はマッチしない (例外的)。 | PYDOC |
    | 194 | マッピングパターン case {'k': v} | dict からの key 抽出。**rest で残り key を辞書として束縛可能。余分な key 許容。 | PYDOC |
    | 195 | クラスパターン case Point(x=0, y=0) | 属性名でマッチ。__match_args__ を定義すると位置引数も使える。dataclass は自動で対応。 | PYDOC |
    | 196 | OR パターン case 1 \| 2 \| 3: | 複数選択肢の論理和。バインド名はすべてのサブパターンで一致が必要。 | PYDOC |
    | 197 | ガード case x if x > 0: | パターン一致後に追加条件。マッチ + 検証を分離するのに便利。 | PYDOC |
    | 198 | AS パターン case [x, y] as pair: | サブパターンと同時に元の値全体を別名にバインドする。 | PYDOC |
    | 199 | 値パターン case Color.RED: | Dotted 名 (定数アクセス) は等価性で比較。Enum メンバの利用が典型。 | PYDOC |

??? note "内包表記 （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 200 | リスト内包 [expr for x in it if cond] | 新リスト生成。複数 for 句 (ネストループ相当) や複数 if 句 (AND 条件) も書ける。 | PYDOC |
    | 201 | 集合内包 {expr for x in it} | 重複排除付きで set を生成。 | PYDOC |
    | 202 | 辞書内包 {k:v for x in it} | 順序保証付き dict を生成。group_by 風や転置に便利。 | PYDOC |
    | 203 | ジェネレータ式 (expr for x in it) | リストを実体化しない iterable を返す。sum(x*x for x in xs) のような遅延評価に使う。 | PYDOC |
    | 204 | ネストした内包 | [y for row in grid for y in row] のように複数 for を連結すると 2 次元を平坦化できる。 | PYDOC |
    | 205 | 条件付き式と内包の組合せ | [(x if x>=0 else -x) for x in xs] のように式の中で三項演算子を使える。 | PYDOC |
    | 206 | 内包内のセイウチ演算子 | [y for x in xs if (y := f(x)) is not None] で副作用付き計算結果を再利用できる。 | PYDOC |
    | 207 | 内包内のローカルスコープ | リスト/辞書/集合内包およびジェネレータ式は専用のローカルスコープを持つ。外側の変数はキャプチャされるが代入は漏れない。 | PYDOC |

??? note "with 文 （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 208 | with context as var: の概要 | コンテキストマネージャを使ったリソース獲得・解放の構文。__enter__ と __exit__ が呼ばれる。 | PYDOC |
    | 209 | with open(...) as f: | ファイルクローズを保証。f.close() を明示的に呼ぶ必要が無くなる。 | PYDOC |
    | 210 | with 複数コンテキスト with A() as a, B() as b: | PEP 617 の新パーサ採用後、括弧で囲んで複数行に分割も可能 (PEP 617)。 | PYDOC |
    | 211 | contextlib.contextmanager デコレータ | yield 文の前で setup、後で teardown を書くだけで CM を自作できる。 | PYDOC |
    | 212 | contextlib.suppress(Exception) | 指定例外を黙って握り潰す。短い try/except pass の代替。 | PYDOC |
    | 213 | contextlib.redirect_stdout(io) | with 内の標準出力を別ストリームへ。テスト時のキャプチャに使う。 | PYDOC |
    | 214 | contextlib.ExitStack | 動的に複数 CM を積めるスタック。条件付きで CM を追加したい場合に。 | PYDOC |
    | 215 | async with | async コンテキストマネージャ (__aenter__/__aexit__) と組み合わせる。asyncio.Lock などで使用。 | PYDOC |

??? note "try 文 （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 216 | try/except 基本 | try ブロックで例外が起きたら最初に一致する except 節で処理する。 | PYDOC |
    | 217 | except 例外型 as e: | e に例外インスタンスを束縛。except 節終了時に名前は削除される (細かな仕様)。 | PYDOC |
    | 218 | 複数 except とタプル | except (KeyError, IndexError) as e: で複数型を 1 節で受ける。 | PYDOC |
    | 219 | try/except/else | try が成功した場合のみ else を実行する。例外を握りつぶさずに「成功時処理」を分離できる。 | PYDOC |
    | 220 | try/finally | 例外の有無にかかわらず必ず finally を実行。リソース解放に。with の方が簡潔。 | PYDOC |
    | 221 | raise / raise from | 例外を投げる。raise X from Y で原因チェーンを明示できる。 | PYDOC |
    | 222 | bare except: の禁止理由 | BaseException も拾うため KeyboardInterrupt や SystemExit まで握り潰す。except Exception: を使う。 | PYDOC |
    | 223 | except* と ExceptionGroup (3.11+) | PEP 654。並列で発生した複数例外をまとめて捕捉する。asyncio.TaskGroup 等で活用。 | PYDOC |
    | 224 | assert 文 | 条件が偽なら AssertionError。-O 起動でスキップされるためビジネスロジックに使ってはいけない。 | PYDOC |

??? note "ジェネレータ （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 225 | yield 文の意味 | 関数本体に yield があるとジェネレータ関数になり呼び出し時に generator iterator を返す。 | PYDOC |
    | 226 | yield from sub_gen | サブジェネレータに走査と send/throw を委譲する。PEP 380。 | PYDOC |
    | 227 | 送信 .send(value) | ジェネレータに値を送り込む。協調的タスクスケジューラの基盤として歴史的に使われた。 | PYDOC |
    | 228 | .close() / .throw() | ジェネレータを終了させる / 例外を内部に投げ込む。 | PYDOC |
    | 229 | ジェネレータ式 vs リスト内包 | 前者は遅延評価でメモリ消費が小さい、後者は一括生成して再利用可能。 | PYDOC |

??? note "非同期 （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 230 | async def コルーチン | async def で定義した関数は呼び出すとコルーチンオブジェクトを返す。実行には event loop または await が必要。 | PYDOC |
    | 231 | await 式 | コルーチン / Future / Task の完了を待つ。async def の中だけ使える。 | PYDOC |
    | 232 | async for / async with | 非同期イテレータ / 非同期 CM 用の構文。 | PYDOC |
    | 233 | コルーチン関数 vs 通常関数 | コルーチンは即時実行されない。await またはイベントループ経由でスケジュールされて初めて走る。 | PYDOC |
    | 234 | async ジェネレータ async def + yield | async for で消費する非同期ジェネレータ。aiterator を返す。 | PYDOC |

??? note "global / nonlocal （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 235 | global x | ブロック内で x への代入がグローバル変数を指すと宣言する。読み取りだけなら不要。 | PYDOC |
    | 236 | nonlocal x | 閉囲する関数スコープの x を指すと宣言する。閉包内での代入に必要。 | PYDOC |
    | 237 | global は関数末尾でも宣言可だが PEP 8 違反 | 宣言は関数先頭にまとめる。コード可読性のため。 | PYDOC |

??? note "制御フロー一般 （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 238 | early return / guard clause パターン | 条件不一致なら if + return で早期脱出し、本処理のネストを浅く保つ。 | PYDOC |
    | 239 | EAFP (Easier to Ask Forgiveness than Permission) | try でやってみて例外で対処する Python 流儀。事前 hasattr 等の LBYL より好まれる場面が多い。 | PYDOC |
    | 240 | LBYL (Look Before You Leap) | 事前にチェックしてから実行する流儀。race condition や時間差で破綻するため EAFP が好まれる。 | PYDOC |
    | 241 | 副作用と純粋関数 | ループの中で副作用を出すと並列化/テストが困難になる。可能なら map/filter/comprehension に分離する。 | PYDOC |
    | 242 | ステートマシンと match-case | 状態遷移を if-elif 連鎖で書くより match-case にすると意図が明確。Enum + dataclass との組合せが強力。 | PYDOC |
    | 243 | loop unrolling と Pythonic | Python ではループ展開より組み込み (sum/any/all/zip) の利用が高速。インタプリタオーバーヘッド削減。 | PYDOC |
    | 244 | ifexpr とジェネレータの組合せ | max(scores, key=lambda s: s.value if s.valid else -1) のような複合パターンが多用される。 | PYDOC |

??? note "ループの効率化 （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 245 | list.append vs += | for ループ + append が list += list よりもオーバーヘッドが少ない場合がある。逆もあり。プロファイルが必要。 | PYDOC |
    | 246 | ローカル変数化 | ホットループ内で組み込み (len, append) をローカル変数に束縛しておくと属性検索が省ける微最適化。 | PYDOC |
    | 247 | itertools 経由の置換 | ループ内のフィルタや結合を itertools.chain/filterfalse/dropwhile などに置き換えると簡潔。 | PYDOC |
    | 248 | Python ループより C 実装組み込み | map/filter/sum/min/max などはバイトコードでなく C で走るので速い。 | PYDOC |

??? note "条件式の慣用句 （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 249 | x or default 値 | x が falsy ならデフォルトを返す。0/'' なども falsy なため意図せず差し替わる落とし穴に注意。 | PYDOC |
    | 250 | x if x is not None else default | None 限定のフォールバック。0 や '' をデフォルトに置換しない。 | PYDOC |
    | 251 | boolean indexing による分岐 | result = (a, b)[cond] のような書き方は副作用が両方評価される。三項演算子か if 文を推奨。 | PYDOC |
    | 252 | all(map(cond, xs)) と any(...) | シンプルなループの代替。遅延評価で短絡。 | PYDOC |
    | 253 | dict.get(k, default) で if 不要 | 存在チェック + 値取り出しを 1 行で。getdefault やデフォルト値の活用。 | PYDOC |
    | 254 | set.issubset と部分集合判定 | for 全件チェックを if subset.issubset(superset): に置換できる。 | PYDOC |

??? note "ステートメント全般 （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 255 | return 文 | 関数の値を呼び出し元に返す。return 無しは return None と同じ。 | PYDOC |
    | 256 | yield 文 (再掲) | ジェネレータ用。return との混在も可能で return 値は StopIteration.value に乗る。 | PYDOC |
    | 257 | del 文 | 名前束縛解除、コンテナ要素削除、属性削除。 | PYDOC |
    | 258 | import 文 | 文として実行されるため if 文の中などでも書ける (遅延 import)。 | PYDOC |
    | 259 | raise 文の独立利用 | except 節内で引数なし raise は現在の例外を再送出する。 | PYDOC |
    | 260 | assert 文 | 前提条件の宣言。production -O ビルドで除去されるので validation には使わない。 | PYDOC |
    | 261 | pass / continue / break の整理 | pass は何もしない、continue は次イテレーションへ、break はループ脱出。 | PYDOC |
    | 262 | ラベル付き break の不在 | Python は label による break/continue を持たない。例外/関数化で対応するのが定石。 | PYDOC |

??? note "3.12 制御フロー関連 （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 263 | PEP 709 内包表記のインライン化 | リスト/集合/辞書内包の本体を呼び出し元フレームでインライン実行する最適化。約 2 倍高速化のケースも。 | PYDOC |
    | 264 | PEP 657 詳細トレースバック | 例外発生時に式単位で位置情報を出すため f(g(x)) のどこで失敗したか視覚的に分かる。 | PYDOC |
    | 265 | PEP 678 例外への add_note() | exc.add_note('context') で追加情報を後付けできる。チェーン保持しつつコンテキストを増やせる。 | PYDOC |
    | 266 | PEP 668 except* と ExceptionGroup の運用 | asyncio.TaskGroup や複数子タスクの並列例外を分けて捕まえるのが用途。 | PYDOC |
