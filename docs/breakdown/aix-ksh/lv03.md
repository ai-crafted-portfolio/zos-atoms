# ODM (Object Data Manager)

**製品**: AIX + ksh ／ **本ページ件数**: 133 件 ／ **出典区分**: 書籍出典

???+ note "環境変数 （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 413 | ODMDIR | ODM コマンドが既定で参照するオブジェクトクラス格納ディレクトリを指す環境変数。未設定なら /etc/objrepos が使われる。 | AIX73 |
    | 414 | ODMDIR の優先順位 | odmget 等は明示的なパス指定がない場合 $ODMDIR を見るので、複数 root の運用ではシェル単位で export する必要がある。 | AIX73 |
    | 415 | ODMDIR と SMIT/cfgmgr | cfgmgr や SMIT が裏で実行する ODM 操作も ODMDIR の影響を受けるため、不用意に変更するとデバイス構成が壊れる。 | — |

??? note "リポジトリ （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 416 | /etc/objrepos | システム固有 (この LPAR/ホスト固有) のカスタマイズ済みデバイス情報を持つ ODM リポジトリ。CuAt, CuDv, Config_Rules 等の Cu* 系がここに置かれる。 | AIX73 |
    | 417 | /usr/lib/objrepos | /usr 共有可能な定義済みデバイス情報を持つ ODM リポジトリ。PdAt, PdDv, PdCn 等の Pd* 系の本体がここに置かれる。 | AIX73 |
    | 418 | /usr/share/lib/objrepos | アーキテクチャ非依存で共有可能な定義情報用 ODM リポジトリ。NLS 関連やヘルプ等が置かれる。 | AIX73 |
    | 419 | リポジトリ分割の目的 | /etc は LPAR 固有、/usr/lib は同一アーキ間で共有、/usr/share は完全共有という階層分離で、ディスクレス/SP 構成時の共有を可能にしている。 | AIX73 |
    | 420 | ODMDIR 既定値 | 未設定時の既定は /etc/objrepos。Cu* 系を直接触りたいときは ODMDIR 設定不要だが、Pd* を触るときは /usr/lib/objrepos を指す必要がある。 | AIX73 |

??? note "odmcreate （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 421 | 概要 | クラス記述ファイル (.cre) を入力に取り、ODM オブジェクトクラスのファイル (.c/.h と空のクラスファイル) を生成するコマンド。 | AIX73 |
    | 422 | 入力ファイル拡張子 | 通常 .cre を使う。class 定義 (class_name { ... }) を含むテキスト形式。 | AIX73 |
    | 423 | -p オプション | 生成される .c/.h ファイルの出力先ディレクトリを指定する。 | AIX73 |
    | 424 | -c オプション | C ヘッダー (.h) のみを生成し、実クラスファイルは作らない (定義検証用)。 | AIX73 |
    | 425 | -h オプション | ヘルプを表示する (短い使用法のみ)。 | — |
    | 426 | 生成物 | クラスごとに ODMDIR 配下のオブジェクトクラスファイル、および C プログラム連携用の .c/.h ファイルが生成される。 | AIX73 |

??? note "odmdrop （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 427 | 概要 | 指定した ODM オブジェクトクラスを完全に削除する破壊的コマンド。クラスファイル自体が消えるので CuAt/CuDv 等の本番系には絶対に使わない。 | AIX73 |
    | 428 | -o オプション | 削除対象のオブジェクトクラス名を指定する。 | AIX73 |
    | 429 | 戻り値 | クラスが存在しない場合は失敗 (非 0) を返す。スクリプトで使う際は事前存在チェック必須。 | — |

??? note "odmshow （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 430 | 概要 | 指定したオブジェクトクラスの構造 (descriptor リスト = 列名と型) を表示する。クラス内のオブジェクト件数や中身は出さない。 | AIX73 |
    | 431 | 使い方 | odmshow ClassName 形式。CuAt や PdDv の構造確認に使う。 | AIX73 |
    | 432 | 出力形式 | C 言語の struct 風表記で descriptor 名, 型 (char, short, long, vchar, method, link) と長さを返す。 | AIX73 |

??? note "odmadd （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 433 | 概要 | stanza 形式の入力ファイルから 1 個以上のオブジェクトをクラスへ追加する。 | AIX73 |
    | 434 | 入力 stanza | ClassName: descriptor = value … の行群。stanza 区切りは空行。 | AIX73 |
    | 435 | 複数オブジェクト追加 | 1 つの入力ファイルに同じクラスの stanza を複数並べると一括追加できる。 | AIX73 |
    | 436 | クラスの自動作成 | odmadd はクラスを作らない。事前に odmcreate で作っておく必要がある。 | AIX73 |

??? note "odmchange （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 437 | 概要 | クエリにマッチする既存オブジェクトを stanza 内容で置換更新する。 | AIX73 |
    | 438 | -o オプション | 対象オブジェクトクラスを指定する。 | AIX73 |
    | 439 | -q オプション | 更新対象を絞り込むクエリ式を指定する。クエリに一致した全オブジェクトが更新対象になる。 | AIX73 |
    | 440 | 入力 stanza の扱い | stanza ヘッダ (Class:) は不要で、descriptor=value 群のみで構成する。 | AIX73 |
    | 441 | 副作用注意 | クエリが甘いと意図しないオブジェクトまで書き換わる。CuAt 等で運用中に実行すると lsattr 結果が変わる。 | — |

??? note "odmdelete （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 442 | 概要 | クエリにマッチする既存オブジェクトをクラスから削除する。クラス自体は残る。 | AIX73 |
    | 443 | -o オプション | 対象オブジェクトクラス名を指定する。 | AIX73 |
    | 444 | -q オプション | 削除対象を絞り込むクエリ式。省略不可 (省略するとクラス全削除の意味で危険)。 | AIX73 |
    | 445 | 戻り値 | 削除件数を stderr に出力する。0 件でも非エラー扱い。 | — |

??? note "odmget （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 446 | 概要 | オブジェクトクラスからクエリにマッチするオブジェクトを取得し、stanza 形式で標準出力に出す。 | AIX73 |
    | 447 | -q オプション | 取得対象を絞り込むクエリ式。省略するとクラス全件を出す。 | AIX73 |
    | 448 | -q の引用 | クエリ式にスペースや特殊文字を含む場合はシェルクォート (シングルクォート推奨) が必要。 | AIX73 |
    | 449 | -d オプション | 出力を stanza 形式ではなく descriptor 単独形式に変える。シェルでパースしやすくなる。 | AIX73 |
    | 450 | -e (修飾子) | リンクオブジェクト経由で参照される下位クラスも展開して取得する (参照先まで再帰展開)。 | — |
    | 451 | クラス未指定時 | クラス名を引数で渡さないと使えない。複数クラスを並べると順に処理する。 | AIX73 |
    | 452 | 典型用途 | odmget -q name=hdisk0 CuAt のように CuAt の特定デバイス属性を抜き出すのが定番。 | AIX73 |

??? note "クエリ言語 （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 453 | 等価 (=) | descriptor=value で完全一致。値が文字列ならクォート ('...') する。 | AIX73 |
    | 454 | 不等価 (!=) | descriptor!=value で不一致。 | AIX73 |
    | 455 | 大小比較 | <, <=, >, >= が数値 descriptor に対して使える。 | AIX73 |
    | 456 | like 演算子 | descriptor like 'pattern' でシェル glob 風 (* と ?) のパターンマッチ。 | AIX73 |
    | 457 | and 演算子 | 複数条件を and で結合する論理積。 | AIX73 |
    | 458 | or 演算子 | 複数条件を or で結合する論理和。 | AIX73 |
    | 459 | not 演算子 | 条件の否定。例: not (attribute=foo)。 | AIX73 |
    | 460 | 括弧の使用 | ( ) で条件式の優先度を明示できる。 | AIX73 |
    | 461 | クエリのクォート規則 | 値の文字列は '...'、descriptor 名は素のまま。シェルから渡すときは全体を "..." で囲み内側に '...' を入れる。 | AIX73 |

??? note "Cu クラス （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 462 | CuDv | Customized Devices クラス。システムに構成済みのデバイスインスタンス (name, status, location, parent, connwhere, PdDvLn) を保持する。lsdev -C の情報源。 | AIX73 |
    | 463 | CuAt | Customized Attributes クラス。CuDv 内デバイスについてデフォルトから変更された属性値のみを保持する。lsattr -El の情報源。 | AIX73 |
    | 464 | CuDep | Customized Dependencies クラス。ボリュームグループとロジカルボリュームのような上下依存関係を保持する。 | AIX73 |
    | 465 | CuDvDr | Customized Device Driver クラス。デバイスのメジャー/マイナー番号とドライバ名のマップを保持する。 | AIX73 |
    | 466 | CuVPD | Customized Vital Product Data クラス。デバイスの VPD (シリアル、FRU 番号等) を保持する。 | AIX73 |
    | 467 | CuPath | MPIO 経路情報クラス。1 デバイスに対する複数パス (parent, connection) を保持する。lspath の情報源。 | AIX73 |
    | 468 | CuPathAt | MPIO パスごとの属性 (priority, path_status 等) を保持するクラス。 | AIX73 |
    | 469 | CuDvDr_PCM | MPIO の Path Control Module 関連の customized 情報を保持する。 | — |
    | 470 | Config_Rules | 起動時の cfgmgr が呼び出す構成メソッドのルール集合。phase1〜3 ごとに実行順を持つ。 | AIX73 |

??? note "Pd クラス （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 471 | PdDv | Predefined Devices クラス。サポートされるデバイス種別の定義 (type, class, subclass, prefix, configure メソッド名等) を持つ。lsdev -P の情報源。 | AIX73 |
    | 472 | PdAt | Predefined Attributes クラス。デバイス種別ごとの属性デフォルト値、属性タイプ (R/N/E/D)、許容範囲を持つ。lsattr -Dl の情報源。 | AIX73 |
    | 473 | PdCn | Predefined Connections クラス。あるデバイス種別がどの親デバイスに、どのコネクションキーで接続できるかを定義する。 | AIX73 |
    | 474 | PdAtXtd | Predefined Attribute Extended クラス。PdAt を補完する SMIT 用追加情報 (ヘルプメッセージ、画面表示名等) を持つ。 | AIX73 |

??? note "SRC クラス （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 475 | SRCsubsys | System Resource Controller のサブシステム定義クラス。subsysname, path, args, action 等を持つ。lssrc / startsrc / stopsrc の情報源。 | AIX73 |
    | 476 | SRCsubsvr | サブサーバ定義クラス。1 つのサブシステム配下に複数のサブサーバを定義できる。 | AIX73 |
    | 477 | SRCnotify | SRC が異常終了等のイベント時に呼び出す通知メソッドを定義するクラス。 | AIX73 |
    | 478 | mkssys との関係 | mkssys/rmssys/chssys は SRCsubsys/SRCsubsvr クラスへの odm 操作のフロントエンド。 | AIX73 |

??? note "NIM （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 479 | nim_object | NIM (Network Installation Manager) は master 上の ODM (nim_attr/nim_pdattr 等) に machines, resources, networks 等のオブジェクトを保持する。 | AIX73 |
    | 480 | Resources | NIM のリソース (lpp_source, spot, mksysb, bosinst_data 等) は ODM オブジェクトとして登録され lsnim で参照される。 | AIX73 |
    | 481 | Network | NIM のネットワーク定義 (ent, tok, atm 等の type と subnet) は ODM 上の networks クラスに保持される。 | AIX73 |
    | 482 | Inventory | NIM クライアントにインストールされた lpp の inventory も ODM オブジェクトとして master 側に保存される。 | — |

??? note "その他クラス （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 483 | vGroup | ボリュームグループ関連の補助 ODM クラス。VG/LV/PP の対応関係をキャッシュする用途。 | — |
    | 484 | Quota | ディスククォータ機能関連の ODM オブジェクトはユーザ/グループ単位のクォータポリシーを保持する (実体は quota.user 等のファイルだが ODM 連動部分あり)。 | — |
    | 485 | Acct | アカウンティング (システム使用量集計) の構成情報の一部が ODM 経由で管理される。 | — |
    | 486 | DSMOptions | Distributed SMIT (DSMIT) 関連オプションを保持する ODM クラス。 | — |
    | 487 | DAVars | Diagnostic Aids 用変数を保持する ODM クラス。diag コマンド系で使用。 | — |

??? note "ロック （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 488 | .lock ファイル | ODM はオブジェクトクラスごとに ClassName.lock ファイルでアドバイザリロックを取り、複数プロセス同時書き込みを排他する。 | AIX73 |
    | 489 | ロック取得粒度 | ロック粒度はクラス単位。同じ CuAt への 2 つの odmchange は直列化される。 | — |
    | 490 | デッドロック回避 | 複数クラスを同時にロックする C API 利用時はクラス順序を統一しないとデッドロック可能性がある。 | — |

??? note "トランザクション （2 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 491 | 書き込みコミット境界 | odmadd/odmchange/odmdelete は 1 オブジェクト単位でフラッシュされる。途中で殺されるとクラスが半端な状態で残りうる。 | — |
    | 492 | savebase | savebase は CuAt/CuDv/Config_Rules 等のブートデバイス関連 ODM を rootvg のブートレコード領域に保存する。bosboot/ブート整合性に必須。 | AIX73 |

??? note "C API （16 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 493 | odm_initialize | ODM ライブラリを初期化する関数。プログラム最初に必ず呼ぶ。 | AIX73 |
    | 494 | odm_terminate | ODM ライブラリの利用を終了し、内部リソースを解放する。 | AIX73 |
    | 495 | odm_open_class | オブジェクトクラスをオープンしてクラス記述子 (CLASS_SYMBOL) を返す。失敗時 -1。 | AIX73 |
    | 496 | odm_close_class | open したクラスをクローズする。 | AIX73 |
    | 497 | odm_get_obj | クエリ式にマッチするオブジェクトを 1 件取得する関数。第 3 引数 returned_data に格納される。 | AIX73 |
    | 498 | odm_get_first | クエリにマッチする最初のオブジェクトを取得する関数。後続は odm_get_next で読む。 | AIX73 |
    | 499 | odm_get_next | 前回 odm_get_first/next のカーソルを進めて次のオブジェクトを取得する。 | AIX73 |
    | 500 | odm_get_list | クエリにマッチする複数オブジェクトを一括取得し配列として返す。 | AIX73 |
    | 501 | odm_add_obj | オブジェクトをクラスへ追加する。成功時は割り当てられた _id を返す。 | AIX73 |
    | 502 | odm_change_obj | 既存オブジェクト (通常 _id 指定) の値を更新する。 | AIX73 |
    | 503 | odm_rm_obj | クエリにマッチするオブジェクトを削除する。戻り値は削除件数。 | AIX73 |
    | 504 | odm_run_method | PdDv 等の method descriptor に登録されているメソッド (configure/unconfigure/start/stop 等) を実行する。 | AIX73 |
    | 505 | odm_mount_class | .c/.h で生成されたクラス記述を実行時に mount し、odm_open_class で使えるようにする。 | AIX73 |
    | 506 | odm_err_msg | 直前の ODM API エラーに対応するメッセージ文字列を取得する。 | — |
    | 507 | odmerrno | 直前の ODM API エラー番号 (errno 風) を保持するグローバル変数。 | AIX73 |
    | 508 | リンクライブラリ | ODM API を使う C プログラムは -lodm でリンクする。ヘッダは <odmi.h>。 | AIX73 |

??? note "stanza （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 509 | 基本形式 | ClassName: の行で始まり、続けて descriptor = value の行が並ぶ。stanza は空行で区切る。 | AIX73 |
    | 510 | 値のクォート | 文字列値は "..." で囲む。改行や = 等の特殊文字を含む場合に必須。 | AIX73 |
    | 511 | 数値型 | short/long descriptor はクォート不要の整数として書く。 | AIX73 |
    | 512 | vchar 型 | 可変長文字列は "..." でクォートして書く。長さは descriptor 定義の最大長以内に収める。 | AIX73 |
    | 513 | method 型 | method descriptor には実行ファイル絶対パスを文字列として書く。実行は odm_run_method 経由。 | AIX73 |
    | 514 | link 型 | 他オブジェクトを参照する descriptor。"ClassName ColumnA ColumnB QueryExpr" の形式で書く。 | AIX73 |
    | 515 | コメント | * で始まる行はコメント扱いされる (odmadd 入力時に限定)。 | — |

??? note "破損/回復 （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 516 | 症状: cfgmgr 失敗 | ODM 破損時の典型症状はブート時 cfgmgr が止まる、lsdev/lsattr が空 or エラー、rootvg のロジカルボリュームが見えない等。 | — |
    | 517 | ODM バックアップ | /etc/objrepos と /usr/lib/objrepos を mksysb 取得前に tar/savevg 等で保護しておくのが基本。 | — |
    | 518 | synclvodm | /etc/objrepos の LVM 情報 (vGroup/CuDv/CuAt 中の LV 関連) を VGDA と再同期するコマンド。LVM 系 ODM 破損時の第一手。 | AIX73 |
    | 519 | redefinevg | VGDA の情報から ODM 上の vGroup を再構成するコマンド。getlvodm/putlvodm と組み合わせて使う。 | AIX73 |
    | 520 | getlvodm/putlvodm | LVM 関連 ODM オブジェクトを直接読み書きする低レベルツール。破損回復で使う。 | — |
    | 521 | savebase の重要性 | ブートに必要な ODM (CuAt/CuDv/CuDep/Config_Rules/CuDvDr/CuVPD/CuPath) は savebase でブートレコードに保存される。bosboot 後は必ず実行されている。 | AIX73 |
    | 522 | ブート時の boot ODM | 起動直後のブート ODM は RAM ディスク上に展開され、root vg マウント後に /etc/objrepos の本体に切り替わる。 | — |

??? note "Config_Rules （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 523 | phase1 | 基盤デバイス (CPU, メモリ, バス, SCSI/FC アダプタ) を構成する初期フェーズ。ブート前半で動く。 | AIX73 |
    | 524 | phase2 | ルートディスク以外の永続デバイスを構成するフェーズ。ブート中盤で動く。 | AIX73 |
    | 525 | phase3 | ブート後のオンライン cfgmgr で動くフェーズ。動的に追加されたデバイスを構成する。 | AIX73 |
    | 526 | seq オペランド | Config_Rules の seq descriptor は同フェーズ内のメソッド実行順を決める数値。小さい方が先。 | AIX73 |
    | 527 | boot_mask | boot_mask descriptor はサービス/ノーマルブート等のモードでルールを有効化するかのマスク。 | — |

??? note "bos_inst （3 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 528 | BOS インストール時の ODM 初期化 | BOS インストールは /usr/lib/objrepos の Pd* (LPP メディアから提供) と空の /etc/objrepos を作り、その後 cfgmgr が Pd* を見ながら Cu* を埋める。 | — |
    | 529 | bosinst_data の役割 | NIM や mksysb での自動インストール時、bosinst_data 内の設定が ODM 初期構成の一部を決める。 | AIX73 |
    | 530 | mksysb 中の ODM | mksysb イメージは /etc/objrepos を含むので、復元時に元のデバイス customization 状態が戻る。 | AIX73 |

??? note "周辺コマンド （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 531 | lsdev | lsdev -C は CuDv を、lsdev -P は PdDv を読んで表示する ODM のフロントエンド。 | AIX73 |
    | 532 | lsattr | lsattr -El は CuAt + PdAt のマージビュー、lsattr -Dl は PdAt のデフォルト値を返す。 | AIX73 |
    | 533 | chdev | chdev は CuAt 上の属性値を更新し、必要なら method を再実行する。バックエンドで odmchange + odm_run_method を呼ぶ。 | AIX73 |
    | 534 | mkdev | mkdev は PdDv 定義を参照して CuDv/CuAt にエントリを追加し define/configure メソッドを実行する。 | AIX73 |
    | 535 | rmdev | rmdev は CuDv/CuAt から該当エントリを消す。-d 付きで完全削除、-d なしで Defined 状態に下げる。 | AIX73 |
    | 536 | cfgmgr | Config_Rules を読み、未構成デバイスについて PdDv → PdAt → PdCn を辿りながら define/configure メソッドを実行して Cu* を埋める。 | AIX73 |
    | 537 | lspath | CuPath を読んで MPIO パス状態を表示するコマンド。 | AIX73 |
    | 538 | lsvpd / lscfg | CuVPD を含む VPD 情報を表示するコマンド。 | AIX73 |
    | 539 | lsitab/mkitab | /etc/inittab を編集するフロントエンド。inittab 自体は ODM ではないが、SRC との連動を理解する上で関連が深い。 | — |

??? note "運用注意 （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 540 | 本番系での直接書き込み禁止 | CuAt/CuDv を odmchange/odmdelete で直接いじると整合性を失う。原則 chdev/mkdev/rmdev を使う。 | — |
    | 541 | バックアップタイミング | デバイス追加・カーネル更新前後で /etc/objrepos のスナップショットを取っておくと事故時の戻しが速い。 | — |
    | 542 | savebase 同期 | ブート ODM (savebase) と稼働 ODM の不整合は次回ブート失敗の原因。chdev 後の bosboot/savebase 実行を忘れない。 | AIX73 |
    | 543 | ODMDIR の取り扱い | ODMDIR を一時的に書き換えるシェルでは、子プロセスへの export 漏れに注意。意図せぬクラスを更新する事故がある。 | — |
    | 544 | クラスファイル直接編集の禁止 | /etc/objrepos 配下のクラスファイルは binary。vi で直接編集してはいけない。 | AIX73 |
    | 545 | 並行 chdev 抑制 | .lock があってもアプリ層で chdev を並行実行すると意図しない順序になる。運用は直列化する。 | — |
