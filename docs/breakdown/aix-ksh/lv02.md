# JFS / JFS2 ファイルシステム

**製品**: AIX + ksh ／ **本ページ件数**: 180 件 ／ **出典区分**: 書籍出典

???+ note "FS作成 - crfs （21 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 233 | crfs 概要 | crfs は /etc/filesystems にスタンザを登録した上で論理ボリュームを作成し JFS/JFS2 ファイルシステムを生成する高水準コマンド。mkfs と異なり LV 未指定でも自動的に LV を確保する。 | AIX73, AIX1 |
    | 234 | crfs -v vfstype | vfs タイプを指定する必須オプション。jfs2 (推奨), jfs, nfs, cdrfs などを指定する。jfs2 は 64bit inode で 16TB 超に対応。 | AIX73, AIX1 |
    | 235 | crfs -g VolumeGroup | ファイルシステムを作成する VG を指定する。-d で既存 LV を指定する場合は -g は不要。-g 指定時は LV が自動作成される。 | AIX73, AIX1 |
    | 236 | crfs -d Device | 既存の論理ボリューム名を指定する。-g と排他。LV は事前に mklv で作成しておく必要があり、crfs はそこに FS 構造のみ書き込む。 | AIX73, AIX1 |
    | 237 | crfs -m MountPoint | マウントポイントを絶対パスで指定する。指定したディレクトリが存在しなければ crfs が自動作成する。/etc/filesystems の mount スタンザのキーになる。 | AIX73, AIX1 |
    | 238 | crfs -A yes\|no | システム起動時 (mount all) に自動マウントするかを指定する。yes で /etc/filesystems に mount = true が書かれる。 | AIX73, AIX1 |
    | 239 | crfs -p rw\|ro | デフォルトの読み書き許可。rw は読み書き、ro は読み取り専用。/etc/filesystems の type フィールドではなく account/options に反映。 | AIX73 |
    | 240 | crfs -a size=Value | ファイルシステムサイズを指定。単位は 512B ブロック、または末尾に M (MB), G (GB), T (TB) を付ける。先頭に + を付けると相対増分指定。例: -a size=2G。 | AIX73, AIX1 |
    | 241 | crfs -a bf=true (JFS only) | JFS で 2GB 超の Large File Enabled ファイルシステムを作成する。JFS2 では常に大ファイル対応のため不要。 | AIX73, AIX1 |
    | 242 | crfs -a logname=LV | JFS/JFS2 ログ用 LV を明示指定する。指定しない場合 VG 内の既定 jfs2log が使用される。INLINE log (JFS2 専用) を使う場合は logname=INLINE を指定する。 | AIX73 |
    | 243 | crfs -a log=INLINE (JFS2) | JFS2 のインラインログ機能。ファイルシステム本体と同一 LV 内にログ領域を埋め込み、別ログ LV を不要にする。-a logsize で領域確保。 | AIX73 |
    | 244 | crfs -a logsize=Value (JFS2) | INLINE ログのサイズを MB 単位で指定。最小 4MB、推奨 0.4% 程度。FS 拡張時のログ枯渇対策に十分な値を確保する。 | AIX73 |
    | 245 | crfs -a ea=v1\|v2 (JFS2) | 拡張属性 (Extended Attributes) のフォーマットバージョン。v2 は ACL/AIXC/NFS4 ACL と任意名前空間の EA をサポート。v1 は古い形式で機能制限あり。 | AIX73 |
    | 246 | crfs -a quota=userquota,groupquota | クォータを有効化するスタンザを追加する。userquota / groupquota / userquota,groupquota を指定可能。quotaon コマンドで実際にクォータ強制を開始する。 | AIX73 |
    | 247 | crfs -a frag/nbpi/ag (JFS only) | JFS 固有チューニング。frag=512/1024/2048/4096 (フラグメントサイズ)、nbpi=N (Number of Bytes Per Inode、小値で inode 多数)、ag=8/16/32/64 (Allocation Group MB)。JFS2 では bsize=4096 固定・inode 動的のため不要。 | AIX73, AIX1 |
    | 248 | crfs -a compress=lzs (JFS2) | JFS2 のオンライン圧縮を有効化。LZS アルゴリズムで透過圧縮。読み取り時に展開、書き込み時に圧縮。CPU 負荷増・I/O 性能低下に注意。 | AIX73, AIX2 |
    | 249 | crfs -a efs=yes (JFS2) | EFS (Encrypted File System) を有効化する。FS 単位で暗号化メタデータを管理し、ユーザ鍵で各ファイル暗号化を行う。 | AIX73 |
    | 250 | crfs -a isnapshot=yes (JFS2) | 内部スナップショット (internal snapshot) 機能を有効化したファイルシステムとして作成する。専用スナップショット LV を不要にする。 | AIX73 |
    | 251 | crfs -a vix=yes (JFS2) | 可変 inode 拡張 (variable inode extension) を許可。inode 領域が動的に拡張される。 | AIX73 |
    | 252 | crfs -a mountguard=yes (JFS2) | PowerHA / 共有 VG 環境で同一 FS の二重マウントを阻止する Mount Guard を有効化。マウント時に他ノード使用を検出すれば失敗させる。 | AIX73 |
    | 253 | crfs -u MountGroup | マウントグループ名を /etc/filesystems の mount スタンザに付与し、mount -t Group で同一グループの FS を一括マウント可能にする。 | AIX73 |

??? note "FS作成 - mkfs （10 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 254 | mkfs 概要 | mkfs は低水準 FS 作成コマンド。LV を事前に mklv で作成しておく必要があり、/etc/filesystems への登録は行わない (crfs と対照的)。 | AIX73, AIX1 |
    | 255 | mkfs -V vfstype | ファイルシステムタイプを指定。jfs (旧 JFS), jfs2 (推奨)。/etc/vfs に定義された vfs エントリ名と一致させる。 | AIX73 |
    | 256 | mkfs -L Label | ボリュームラベルを設定する。lsfs や fsck で参照可能。 | AIX73 |
    | 257 | mkfs -m MountPoint | マウントポイントをメタデータに記録する。/etc/filesystems への登録は行わない。 | AIX73 |
    | 258 | mkfs -s Size | FS サイズを 512B ブロック単位で指定する。LV の物理サイズより小さく設定可能。 | AIX73 |
    | 259 | mkfs -o Options | vfs 固有のオプションを渡す。例: -o nbpi=4096, -o log=/dev/loglv00。 | AIX73 |
    | 260 | mkfs.jfs / mkfs.jfs2 | vfs 種別ごとのフロントエンド。mkfs -V jfs2 と同等。直接呼ぶ場合は mkfs.jfs2 -O log=INLINE のようにオプション付与可。 | AIX73 |
    | 261 | mkfs.jfs2 -O log_partition_count | JFS2 ログ用パーティション数。INLINE ログ使用時に内部割り当て数を制御する。 | AIX73 |
    | 262 | mkfs.jfs2 -O outline / inline log | ログ配置を outline (外部ログ LV) / inline (本体 LV 内) のいずれかで指定。inline はバックアップ・移行が楽。 | AIX73 |
    | 263 | mkfs.jfs2 -O isnapshot=yes | FS 作成時に内部スナップショットを有効化する。 | AIX73 |

??? note "/etc/filesystems （12 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 264 | スタンザ構造 | /etc/filesystems はマウントポイントをスタンザ名としたフラットテキスト。各スタンザは属性 = 値 で記述し、空行で区切る。 | AIX73, AIX1 |
    | 265 | dev 属性 | dev = /dev/lvname の形式で論理ボリュームデバイスを指定する。FS 実体への入口。 | AIX73, AIX1 |
    | 266 | vfs 属性 | vfs = jfs2 / jfs / nfs / cdrfs / procfs / autofs などのファイルシステム種別。/etc/vfs に対応エントリが必要。 | AIX73, AIX1 |
    | 267 | log 属性 | log = /dev/jfs2log (例) で外部ログ LV を指定する。INLINE 時は log = INLINE。 | AIX73 |
    | 268 | mount 属性 | mount = true\|false\|automatic\|removable。true で mount -a 対象、automatic でブート時自動マウント、false で手動のみ。 | AIX73, AIX1 |
    | 269 | check 属性 | check = true\|false\|順序番号。fsck -p 時の検査対象と並列度を制御する。 | AIX73 |
    | 270 | type 属性 | type = MountGroup。mount -t Group で一括マウントするためのグループ名。 | AIX73 |
    | 271 | options 属性 | options = rw,noatime,nodev など mount -o 相当のオプションをカンマ区切りで指定する。 | AIX73 |
    | 272 | account 属性 | account = true\|false。ディスク利用統計 (diskusg) の対象とするか。 | AIX73 |
    | 273 | quota 属性 | quota = userquota,groupquota。クォータ強制対象種別。 | AIX73 |
    | 274 | nodename 属性 (NFS) | nodename = server。NFS マウント時にサーバ FQDN を指定する。 | AIX73 |
    | 275 | free 属性 (JFS) | free = true\|false。FS のフォーマット直後を示すフラグ。 | AIX73 |

??? note "FS変更 - chfs （14 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 276 | chfs 概要 | chfs はマウント済み or 未マウントの JFS/JFS2 ファイルシステム属性を変更する。LV サイズ拡張・mount フラグ変更・ログ変更などに用いる。 | AIX73, AIX1 |
    | 277 | chfs -A yes\|no | 自動マウント属性を変更する。/etc/filesystems の mount 値を書き換える。 | AIX73 |
    | 278 | chfs -a size=Value | ファイルシステム拡張。+ で相対増分指定可 (例: -a size=+1G)。JFS2 ではオンライン拡張が可能。 | AIX73, AIX1 |
    | 279 | chfs -a size=- (オンライン縮小) | JFS2 のみ対応するオンライン縮小。-a size=-1G で 1GB 縮小、または絶対値で目標サイズを指定。inode/ブロック移動が発生するため時間を要する。 | AIX73 |
    | 280 | chfs -m NewMountPoint | マウントポイントを変更する。マウント中は変更不可、umount 後に実行する。 | AIX73 |
    | 281 | chfs -d Attribute | /etc/filesystems から指定属性を削除する。例: chfs -d account /home。 | AIX73 |
    | 282 | chfs -u MountGroup | マウントグループ (type 属性) を変更する。 | AIX73 |
    | 283 | chfs -a logname=LV | FS が使用するログ LV を変更する。アンマウント状態で実行する必要がある。 | AIX73 |
    | 284 | chfs -a quota=... | クォータ種別の有効/無効化。userquota / groupquota / 両方を指定。 | AIX73 |
    | 285 | chfs -a ea=v2 (JFS2) | 拡張属性フォーマットを v1 から v2 へ変換する。一旦 v2 にすると v1 に戻せない。 | AIX73 |
    | 286 | chfs -a splitcopy=/MP -a copy=N (スプリットミラー) | ミラー化された JFS2 FS のオフライン複製を作成する。指定したミラーコピーを切り出して読み取り専用 FS としてマウント可能にする。 | AIX73 |
    | 287 | chfs -a freeze=N / refreeze=N / thaw | FS を瞬時凍結する。バックアップ整合性確保用。freeze=秒数で自動解凍、thaw で即時解除。 | AIX73 |
    | 288 | chfs -a options=... | mount オプション (rw, noatime, log=NULL など) を変更する。 | AIX73 |
    | 289 | chfs -a mountguard=yes/no | Mount Guard を後付け有効化・無効化する。 | AIX73 |

??? note "FS削除/参照 （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 290 | rmfs | ファイルシステムを削除する。/etc/filesystems スタンザ削除 + LV 削除を一括実行する高水準コマンド。事前 umount 必須。 | AIX73, AIX1 |
    | 291 | rmfs -r | マウントポイントディレクトリも併せて削除する。 | AIX73 |
    | 292 | rmfs -i | 実行確認プロンプトを出す対話モード。 | AIX73 |
    | 293 | lsfs | ファイルシステム一覧表示。マウントポイント / LV / vfs / サイズ / オプションを列挙する。 | AIX73, AIX1 |
    | 294 | lsfs -q | JFS/JFS2 詳細属性 (フラグメントサイズ, EA バージョン, ログ LV, 圧縮/暗号化フラグなど) を表示する。 | AIX73 |
    | 295 | lsfs -v vfstype | 指定種別の FS のみ列挙する。例: lsfs -v jfs2。 | AIX73 |
    | 296 | lsfs -a / -c / -l | 出力フォーマット切替。-c はコロン区切り、-l は long リスト。 | AIX73 |

??? note "mount/umount （19 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 297 | mount 単独実行 | mount /mountpoint で /etc/filesystems のスタンザを参照し当該 FS をマウントする。 | AIX73, AIX1 |
    | 298 | mount -a | /etc/filesystems で mount=automatic\|true の全 FS をマウント。起動時 rc.boot で実行される。 | AIX73 |
    | 299 | mount -t MountGroup | type 属性が一致する FS 群を一括マウントする (PowerHA リソースグループ運用などで多用)。 | AIX73 |
    | 300 | mount -V vfstype | vfs 種別を明示する (例: mount -V cdrfs)。/etc/filesystems に登録のない FS を直接マウント可能。 | AIX73 |
    | 301 | mount -o options | mount オプション。rw / ro / log=NULL / nointegrity / noatime / nodev / nosuid / rbrw など。カンマ区切り。 | AIX73 |
    | 302 | mount -o log=NULL (JFS2) | ログを無効にして非整合性マウント。一時 FS 性能向上用 (再起動時に内容失われる前提)。 | AIX73 |
    | 303 | mount -o nointegrity (JFS2) | ログを書かずに性能優先。クラッシュ時 FS 破損の覚悟が必要。tmp 用途。 | AIX73 |
    | 304 | mount -o noatime | アクセス時刻更新を抑止。大量参照ワークロードでメタデータ書込を削減し性能を改善する。 | AIX73 |
    | 305 | mount -o rbrw / rbr | Release Behind on Read / Write。シーケンシャル I/O ページキャッシュを早期解放しメモリ圧迫を軽減。 | AIX73 |
    | 306 | mount -o cio | Concurrent I/O。DIO + iノードロックバイパス。Oracle 等 DB の二重キャッシュ回避用。 | AIX73, AIX2 |
    | 307 | mount -o dio | Direct I/O。ファイルシステムキャッシュをバイパス。 | AIX73 |
    | 308 | mount -r / -o ro | 読み取り専用マウント。-r は -o ro のショートカット。書き込みは EROFS で拒否される。 | AIX73 |
    | 309 | mount (引数なし) | 現在マウント中の全 FS 一覧を表示。出力は node, mounted, mounted over, vfs, date, options 列。 | AIX73, AIX1 |
    | 310 | umount /mountpoint | ファイルシステムをアンマウント。使用中ファイルがあると EBUSY。 | AIX73, AIX1 |
    | 311 | umount -f | 強制アンマウント (JFS2 一部)。使用中でも切り離す。NFS スタール時に有効。 | AIX73 |
    | 312 | umount -t/-V / unmount | umount -t Group は type 属性単位、-V vfstype は種別単位で一括アンマウント。unmount は umount の別名 (同一バイナリ)。 | AIX73 |
    | 313 | fuser /mountpoint | FS を使用中のプロセス PID を列挙する。umount EBUSY 時のトラブルシュート定番。 | AIX73, AIX1 |
    | 314 | fuser -k /mountpoint | 使用中プロセスに SIGKILL を送信する。umount 直前の強制クローズ用。 | AIX73 |
    | 315 | fuser -cu | FS 全体検査 (-c) とユーザ名表示 (-u) の組合せ。-x で実行中バイナリ保持プロセスも列挙し text file busy 解消に使う。 | AIX73 |

??? note "JFS2拡張機能 （16 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 316 | 拡張属性 EA v1/v2 | JFS2 の拡張属性。v1 は固定領域・サイズ制限あり、v2 は inode 動的拡張で任意名前空間 EA を保持。ACL や SELinux 風メタデータの基盤。 | AIX73 |
    | 317 | AIXC ACL | AIX Classic ACL。所有者/グループ/その他に加え extended permissions を持つ。aclget / aclput / acledit で操作。 | AIX73, AIX1 |
    | 318 | NFS4 ACL | NFSv4 形式の ACL。allow/deny ACE、継承フラグ (file_inherit/dir_inherit) を持つ。EA v2 必須。 | AIX73 |
    | 319 | aclget | ファイル/ディレクトリの ACL を取得し標準出力へ表示する。 | AIX73 |
    | 320 | aclput | 標準入力から ACL 定義を読み込み設定する。 | AIX73 |
    | 321 | acledit | $EDITOR を起動して ACL を対話編集する。 | AIX73 |
    | 322 | ファイルセット (filesets) | JFS2 内部の論理パーティション。クォータ・スナップショットを独立して制御できる単位。-a fileset_quota で個別容量制限。 | AIX73, AIX2 |
    | 323 | 暗号化 EFS | Encrypted File System。ユーザ鍵ストア (efskeymgr) を用い AES-128/192/256 で透過暗号化。efsenable で初期化、ファイル単位有効化。 | AIX73 |
    | 324 | EFS 管理コマンド一式 | efsenable で鍵ストア初期化、efskeymgr でユーザ鍵管理 (生成・パスフレーズ変更)、efsmgr -e でファイル単位暗号化開始 / -l で状態確認。 | AIX73 |
    | 325 | オンライン拡張 | chfs -a size=+1G のように稼働中の JFS2 を縮小なしで拡張可能。LV 拡張 → FS 認識を一括で実施する。 | AIX73, AIX1 |
    | 326 | オンライン縮小 | JFS2 のみ対応。空き領域があれば chfs -a size で縮小可能。実装上 LV 末端のブロックを使用領域へ移動する。 | AIX73 |
    | 327 | 外部スナップショット | snapshot -o snapfrom=/fs -o size=100M で別 LV にスナップを取得。読み取り専用マウントして backup 等の整合点として利用。 | AIX73, AIX2 |
    | 328 | 内部スナップショット | isnapshot=yes の FS で snapshot -o snapfrom コマンドだけで取得可能。専用 LV 不要、最大 64 世代。 | AIX73 |
    | 329 | snapshot コマンド | JFS2 スナップショットの作成/削除/一覧。-o snapfrom (元), -o size (サイズ), -d (削除), -q (一覧)。 | AIX73 |
    | 330 | rollback (chfs -a) | スナップショット世代に戻す。chfs -a rollback=snapname /fs。 | AIX73 |
    | 331 | クォータ JFS2 | ユーザ/グループに加えファイルセット単位クォータをサポート。j2edquota / quotacheck -t jfs2 を使用。 | AIX73 |

??? note "整合性・最適化 （16 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 332 | defragfs | JFS/JFS2 の断片化を解消する。マウント中・読み書き中でも実行可能 (オンラインデフラグ)。 | AIX73, AIX1 |
    | 333 | defragfs -q | デフラグせず断片化レポートのみ出力 (query mode)。 | AIX73 |
    | 334 | defragfs -r | レポート + デフラグ実行。 | AIX73 |
    | 335 | fsck 概要 | ファイルシステム整合性検査・修復。FS をアンマウントしてから実行するのが原則。JFS2 はジャーナル再生で大半が自動回復するため通常不要。 | AIX73, AIX1 |
    | 336 | fsck -y | 全プロンプトに自動 yes 応答。バッチ復旧用。 | AIX73, AIX1 |
    | 337 | fsck -n | 全プロンプトに自動 no 応答。読み取り専用に動作する diagnostic mode。 | AIX73 |
    | 338 | fsck -p | Preen mode。軽微な不整合のみ自動修復、対話なし。rc.boot から自動呼び出しされる。 | AIX73 |
    | 339 | fsck -V vfstype | vfs 種別指定 (jfs / jfs2)。混在環境で誤検査を避けるため使用。 | AIX73 |
    | 340 | fsck -o options | vfs 固有検査オプション。例: -o nolog でログ再生を無効。 | AIX73 |
    | 341 | fsck -f | クリーン (前回正常アンマウント) であっても強制的に検査する。 | AIX73 |
    | 342 | fsck -t File | 一時作業ファイルパス指定。/tmp が小さい際に明示指定する。 | AIX73 |
    | 343 | tunefs | FS チューニング属性を表示・変更する。fragmentation や inode 管理の調整。JFS2 では限定的。 | AIX73 |
    | 344 | tunefs -A | ファイルシステムの ag (Allocation Group) 関連属性を調整する。 | AIX73 |
    | 345 | tunefs -B | JFS のブロックサイズ系属性表示。 | AIX73 |
    | 346 | fsdb | ファイルシステムデバッガ。inode/superblock/dirent をブロック単位で確認・編集できる低レベルツール。緊急復旧用。 | AIX73 |
    | 347 | fsdb 起動 | fsdb /dev/lvname の形で起動。? でヘルプ、quit で終了。誤操作で FS を破壊する危険性が高いため熟練者専用。 | AIX73 |

??? note "LV/FS対応 （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 348 | /dev/hd? と既定 FS | rootvg の標準 LV 命名: /dev/hd4=/, /dev/hd2=/usr, /dev/hd3=/tmp, /dev/hd9var=/var, /dev/hd1=/home, /dev/hd11admin=/admin, /dev/hd10opt=/opt。 | AIX73, AIX1 |
    | 349 | /dev/hd8 (ログ LV) | rootvg 既定 JFS2 ログ LV。jfs2log タイプの LV。FS は持たない。 | AIX73, AIX1 |
    | 350 | /dev/hd5 / hd6 / hd7 | hd5=boot LV, hd6=paging space (デフォルト), hd7=ダンプ用。FS としては使わない。 | AIX73, AIX1 |
    | 351 | /dev/loglv00 等の自動命名 | ユーザ作成 VG で crfs が自動生成するログ LV の既定名。複数 VG で同名が重複しないよう連番付与。 | AIX73 |
    | 352 | LV タイプと FS の対応 | LV タイプ (lslv -L で確認) は jfs / jfs2 / jfslog / jfs2log / paging / boot / sysdump 等。crfs/chfs が自動設定する。 | AIX73 |

??? note "backup/restore （15 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 353 | backup -i (個別ファイル) | 標準入力から取得したファイルリストを名前ベースでバックアップする。find と組合せて差分バックアップ可能。 | AIX73, AIX1 |
    | 354 | backup -Level filesystem (i-node) | i-node ベースの完全/差分バックアップ。レベル 0〜9 を指定し、レベル N は直近のレベル N-1 より前以来の更新を取得する。 | AIX73, AIX1 |
    | 355 | backup -f Device | 出力先デバイス指定。テープ (/dev/rmt0) や通常ファイル、パイプも指定可。 | AIX73, AIX1 |
    | 356 | backup -p (パック) | 通常ファイルを pack で圧縮しながらアーカイブする。 | AIX73 |
    | 357 | backup -u | /etc/dumpdates を更新し、レベル別バックアップ履歴を記録する。差分判定の基準となる。 | AIX73, AIX1 |
    | 358 | backup -L Length | テープ 1 巻あたりの長さ (バイト) を指定する。マルチボリュームバックアップで使用。 | AIX73 |
    | 359 | backup -v (詳細) | 処理中のファイル名を逐次表示する。 | AIX73 |
    | 360 | backup -b BlockSize | ブロックサイズ (1024B 単位) 指定。テープ性能・互換性に影響。 | AIX73 |
    | 361 | restore -x | アーカイブからファイルを取り出す (extract)。-d で指定ディレクトリ配下にも。 | AIX73, AIX1 |
    | 362 | restore -T | アーカイブ内容の一覧表示。 | AIX73 |
    | 363 | restore -t (タイムスタンプ確認) | より詳細なメタデータ付き一覧表示。 | AIX73 |
    | 364 | restore -i (対話) | 対話モードでファイルを選択的にリストアする。add / extract / quit などのサブコマンドあり。 | AIX73 |
    | 365 | restore -r (i-node 全復元) | i-node 形式バックアップ全体を空 FS にリストアする。新規 FS への完全復旧用。 | AIX73 |
    | 366 | restore -f Device / -B (stdin) | 入力デバイス指定 / 標準入力読み込み。 | AIX73 |
    | 367 | /etc/dumpdates | backup -u で更新されるレベル別バックアップ日時履歴。差分バックアップの基準。 | AIX73 |

??? note "アーカイブ （10 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 368 | pax 概要 | POSIX 標準のアーカイブツール。tar / cpio 両形式を読み書きでき AIX 推奨。-r/-w/-rw でモード指定。 | AIX73 |
    | 369 | pax -w (write) | アーカイブ作成モード。標準出力または -f File へ書き出す。 | AIX73 |
    | 370 | pax -r (read) | アーカイブ展開モード。-f File 指定で対象指定。 | AIX73 |
    | 371 | pax -x Format | アーカイブ形式指定。ustar / cpio / pax。 | AIX73 |
    | 372 | cpio -o (出力) | 標準入力のファイル名リストからアーカイブ生成。find と組合せる。 | AIX73 |
    | 373 | cpio -i (入力) | アーカイブ展開モード。-d でディレクトリ自動作成、-m でタイムスタンプ保持、-v で詳細。 | AIX73 |
    | 374 | cpio -p (パススルー) | ファイルを別ディレクトリへコピー (アーカイブを介さない高速複製)。 | AIX73 |
    | 375 | tar -c / -x / -t | tar 作成 / 展開 / 一覧。-v で詳細、-f で対象指定、-z は AIX 標準 tar には無いため gzip と組合せる。 | AIX73 |
    | 376 | tar -f Device | アーカイブファイル/デバイス指定。- で標準入出力。 | AIX73 |
    | 377 | tar -p | permissions を保持して展開する。所有者を含む属性をできる限り保つ。 | AIX73 |

??? note "NFS （16 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 378 | mount -V nfs | NFS マウントの基本形。mount -V nfs server:/export /mnt のように指定する。 | AIX73 |
    | 379 | mount -o vers=3 | NFSv3 でマウント。UDP/TCP, ファイルロックは rpc.lockd 経由。 | AIX73 |
    | 380 | mount -o vers=4 | NFSv4 マウント。stateful、組み込み ACL、コンパウンド RPC、TCP のみ。 | AIX73 |
    | 381 | mount -o hard / soft | hard はサーバ応答まで永久リトライ (DB 用), soft はタイムアウトで EIO 返却 (応答性優先)。 | AIX73 |
    | 382 | mount -o intr | hard マウント時でも割り込み (^C) で処理中断を許可する。 | AIX73 |
    | 383 | mount -o bg/fg/rsize/wsize | bg: 起動時マウント失敗をバックグラウンドリトライ、fg: 即時失敗。rsize/wsize は RPC 読み書き 1 回の最大バイト数で性能チューニングの中心。 | AIX73 |
    | 384 | mount -o proto=tcp/udp | RPC トランスポートプロトコル。NFSv4 は tcp のみ。 | AIX73 |
    | 385 | mount -o sec=... | セキュリティモード。sys (UID マッピング), krb5 / krb5i / krb5p (Kerberos 認証/整合性/秘匿)。 | AIX73 |
    | 386 | mount -o nosuid / nodev | クライアント側で setuid バイナリやデバイスファイルを無効化する。 | AIX73 |
    | 387 | /etc/exports | NFS サーバのエクスポート定義ファイル。/path -opts,client=node1:node2 形式。 | AIX73 |
    | 388 | exportfs -a | /etc/exports を読み込み全エントリをエクスポート反映する。 | AIX73 |
    | 389 | exportfs -u | 指定エクスポートを取り消す。-au で全エクスポート停止。 | AIX73 |
    | 390 | exportfs -v / -i | 現在のエクスポート状態確認 / /etc/exports を無視して直接エクスポート。 | AIX73 |
    | 391 | mknfsmnt / chnfsmnt / rmnfsmnt | NFS マウントを /etc/filesystems スタンザとして作成・変更・削除する高水準コマンド。 | AIX73 |
    | 392 | showmount -e server | リモート NFS サーバのエクスポート一覧を確認する。 | AIX73 |
    | 393 | automount / autofs | オンデマンドマウント。/etc/auto_master と各マップで指定。アイドル時自動アンマウント。 | AIX73 |

??? note "CacheFS （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 394 | CacheFS 概要 | NFS マウントのキャッシュレイヤ。リモート FS のブロックをローカル FS にキャッシュし読み取り高速化。AIX 7.3 では一部機能制限あり。 | AIX73 |
    | 395 | cfsadmin -c | キャッシュディレクトリを作成する。-o maxblocks=N% などでサイズ上限を設定。 | AIX73 |
    | 396 | cfsadmin -l | キャッシュ統計と構成を表示する。 | AIX73 |
    | 397 | mount -V cachefs | CacheFS としてリモート FS をマウントする。backfstype=nfs と cachedir を指定。 | AIX73 |
    | 398 | fsck_cachefs | CacheFS キャッシュ整合性検査。 | AIX73 |

??? note "AIO （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 399 | AIO 概要 | Asynchronous I/O。アプリケーションが I/O 完了を待たずに次処理を進める仕組み。Oracle 等の DB ワークロードで性能向上に必須。 | AIX73, AIX2 |
    | 400 | Legacy AIO vs POSIX AIO | AIX には旧来の aio_* と POSIX 準拠 aio_* の 2 系統がある。AIX 6.1 以降 fastpath が標準。 | AIX73 |
    | 401 | ioo -o aio_maxreqs | AIO 同時要求の最大数。デフォルト 65536。 | AIX73 |
    | 402 | ioo -o aio_maxservers | AIO カーネルサーバスレッドの上限値。負荷に応じ動的増減する。 | AIX73 |
    | 403 | ioo -o aio_minservers | AIO カーネルサーバスレッドの最小値 (常時起動数)。 | AIX73 |
    | 404 | ioo -o aio_server_inactivity | アイドルなサーバスレッドが終了するまでの秒数。 | AIX73 |
    | 405 | pstat -a \| grep aios | 実行中の AIO サーバスレッド数の確認。 | AIX73 |
    | 406 | fastpath AIO | JFS2/raw LV 向けに kproc を介さず直接 LVM へ I/O を投入する高速経路。aio_fastpath=1 がデフォルト。 | AIX73, AIX2 |
    | 407 | CIO + AIO の組合せ | Concurrent I/O マウントの JFS2 上で AIO を使用すると DB の二重キャッシュを排除しつつ非同期 I/O 並列度を確保できる。 | AIX73, AIX2 |

??? note "クォータ （5 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 408 | quotaon / quotaoff | FS のクォータ強制を開始/停止する。マウント済み FS に対して実行。 | AIX73 |
    | 409 | quotacheck | FS を走査し quota ファイルを再構築する。クォータ有効化前に実行する。 | AIX73 |
    | 410 | edquota / j2edquota | ユーザ/グループ/ファイルセットのクォータ制限を編集する。j2edquota は JFS2 専用で fileset 単位制限が可能。 | AIX73 |
    | 411 | quota コマンド | ユーザ視点で自身の使用量と制限を確認する。 | AIX73 |
    | 412 | repquota | FS 全体のクォータ使用状況をユーザ別に集計表示する。 | AIX73 |
