# 診断 (errpt/snap) + PowerHA

**製品**: AIX + ksh ／ **本ページ件数**: 216 件 ／ **出典区分**: 書籍出典

???+ note "errpt （26 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1893 | errpt | AIX のエラーログ表示コマンド。errdemon が /var/adm/ras/errlog に書いた二進ログを整形して標準出力に出す。オペランド無しでサマリ一覧 (識別子/タイムスタンプ/タイプ/クラス/リソース/説明) を表示。 | AIX2, AIX73 |
    | 1894 | errpt -a | Detailed format。各エントリのテンプレートに従い詳細表示。原因/推奨処置/障害データ (Detail Data) を全部出す。サポート問い合わせの基本情報。 | AIX2, AIX73 |
    | 1895 | errpt -d <ErrorClass> | エラークラスでフィルタ。H=Hardware (PdAt 上のハードウェア)、S=Software、O=Operator (errlogger 由来)、U=Undetermined を組合せ可。例 -d H,S。 | AIX2, AIX73 |
    | 1896 | errpt -j <ErrorID> | 指定 ID (8桁16進、例 AA8AB241) のエントリだけ表示。-j A,B,C のようにカンマ列挙可。errpt -a -j <ID> がトラブルシュート定番。 | AIX2, AIX73 |
    | 1897 | errpt -k <ErrorID> | -j の逆。指定 ID を除外して表示。よく出るがどうでもいい情報 (例 SRC リスタート) を黙らせるのに使う。 | AIX2, AIX73 |
    | 1898 | errpt -t | テンプレートを表示。エラーログ ID 毎に定義された Label / Class / Type / Description を確認する。errdemon が参照する /etc/objrepos/errtmplt と対応。 | AIX2, AIX73 |
    | 1899 | errpt -l <SequenceNumber> | シーケンス番号 (errdemon が付与する連番) でエントリ指定。-a と組み合わせて 1 件詳細表示。 | AIX2, AIX73 |
    | 1900 | errpt -N <ResourceName> | リソース名 (例 hdisk0 / ent0 / sysplanar0) でフィルタ。-N hdisk0 で当該ディスクのエラーだけ。 | AIX2, AIX73 |
    | 1901 | errpt -n <ResourceClass> | リソースクラス (例 disk, adapter, memory, processor) でフィルタ。-N より広く絞る。 | AIX2, AIX73 |
    | 1902 | errpt -s <StartDate> | 開始日時を mmddhhmmyy (月日時分年) 形式で指定。例 -s 0601000026 → 2026/06/01 00:00 以降。 | AIX2, AIX73 |
    | 1903 | errpt -e <EndDate> | 終了日時を mmddhhmmyy 形式で指定。-s と -e を併用して期間絞り込み。 | AIX2, AIX73 |
    | 1904 | errpt -T <Type> | エラー種別でフィルタ。PEND (障害発生予兆)、PERF (性能低下)、PERM (回復不能)、TEMP (一時)、UNKN、INFO の組合せ。例 -T PERM,PEND。 | AIX2, AIX73 |
    | 1905 | errpt -A | Concurrent (abbreviated) 形式。1 行サマリの圧縮版。シェル集計で扱いやすい。 | AIX2, AIX73 |
    | 1906 | errpt -V | Very detailed。-a よりさらに詳しくテンプレ情報と詳細データを並べる。 | AIX2, AIX73 |
    | 1907 | errpt -y <file> | 別 errlog バイナリファイル (例 別ホストからコピーした /var/adm/ras/errlog) を読む。スナップショット解析の必須オプション。 | AIX2, AIX73 |
    | 1908 | errlogger "<message>" | オペレータ起源メッセージを errlog に書く。シェルスクリプトから故障マークを残す用途。OPMSG ラベル/O クラスで記録される。 | AIX2, AIX73 |
    | 1909 | errclear <days> | エラーログから N 日より古いエントリを削除。例 errclear 30 → 30 日より古いものを削除。日次 cron に入れて肥大化抑制。 | AIX2, AIX73 |
    | 1910 | errclear -d/-j オプション | クラス指定削除 (errclear -d H 0 でハードウェア全削除) や ID 指定削除 (-j <ID>) で煩い特定種別だけ消す。 | AIX2, AIX73 |
    | 1911 | errstop | errdemon を停止。停止中は errlog 書き込み不可。通常は errdemon を常駐させる。errlog ファイルメンテ時のみ停止。 | AIX2, AIX73 |
    | 1912 | errdemon | エラーログデーモン。/usr/lib/errdemon。boot 時に rc.boot から起動。kernel が posixyieldlog 経由で渡したイベントを /var/adm/ras/errlog に書く。 | AIX2, AIX73 |
    | 1913 | errdemon -B <size> | errlog ファイル最大サイズ設定 (バイト)。既定 1MB。/var/adm/ras/errlog サイズが上限超で巻き戻す。 | AIX2, AIX73 |
    | 1914 | errdemon -l | 現在の errdemon 設定 (errlog パス/サイズ/buffer) 表示。 | AIX2, AIX73 |
    | 1915 | errupdate | エラーテンプレ ODM (errtmplt) を更新。.err ファイル (テンプレ定義) を入力に取り、エラー ID 追加/削除/更新。アプリ側がカスタムエラーを定義する時に使う。 | AIX2, AIX73 |
    | 1916 | errnotify ODM クラス | エラー発生時の自動アクション (メール送信/スクリプト起動) を定義する ODM クラス。/etc/objrepos/errnotify。en_name / en_persistenceflg / en_method 等を持つ。 | AIX2, AIX73 |
    | 1917 | /var/adm/ras/errlog | エラーログ本体 (バイナリ)。errpt はこれを読む。直接編集禁止。サイズ上限は errdemon -B で制御。 | AIX2, AIX73 |
    | 1918 | /var/adm/ras/conslog | コンソール出力のテキストログ。boot 中や hard error 時に console に出たメッセージを保持。 | AIX2, AIX73 |

??? note "syslog （10 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1919 | syslog | BSD 由来のシステムログサービス。kernel/デーモン/アプリが syslog() API でログを送出し syslogd が振り分け。AIX では errpt とは別系統 (errpt はバイナリ ODM テンプレ式、syslog はテキスト式)。 | AIX2, AIX73 |
    | 1920 | syslogd | /usr/sbin/syslogd。SRC で管理される subsystem。startsrc -s syslogd / stopsrc -s syslogd / refresh -s syslogd で操作。/etc/syslog.conf を読む。 | AIX2, AIX73 |
    | 1921 | /etc/syslog.conf | syslogd 設定ファイル。1 行 1 ルールで <facility>.<priority>[;...] <action> 形式。タブ区切り必須 (スペースだと動かない事例あり)。 | AIX2, AIX73 |
    | 1922 | syslog facility | 送信元区分。kern / user / mail / daemon / auth / syslog / lpr / news / uucp / cron / local0..local7 / * (全て)。 | AIX2, AIX73 |
    | 1923 | syslog priority | 重要度。emerg / alert / crit / err / warning / notice / info / debug / none。指定 priority 以上を捕捉。 | AIX2, AIX73 |
    | 1924 | syslog action | 出力先。/path/to/file (絶対パス)、@hostname (UDP 転送)、user1,user2 (端末通知)、* (全ログインユーザ)、\|prog (パイプ)。 | AIX2, AIX73 |
    | 1925 | syslog.conf 記述例 | *.err /var/adm/messages で全 facility の err 以上をファイル保存。*.* @loghost で別ホスト syslogd (UDP 514) へ転送 (集中ログホスト構成)。 | AIX2, AIX73 |
    | 1926 | refresh -s syslogd | syslog.conf 変更後に syslogd へ SIGHUP を送り設定再読込。停止再開せずに済む標準手順。 | AIX2, AIX73 |
    | 1927 | logger コマンド | シェルから syslog にメッセージ送出。logger -p user.info "message" 等で facility.priority 指定。シェルスクリプトの監査痕跡用。 | AIX2, AIX73 |
    | 1928 | rotate オプション | syslog.conf 行末に rotate size <N>M files <N> time <N>d で AIX 固有のローテーション設定可能。logrotate なしで完結。 | AIX73 |

??? note "alog （10 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1929 | alog | AIX 固有の循環ログ機構。boot/console/nim/cfg/dumpsymp 等の種別毎に固定サイズの循環バッファをファイルとして持つ。サイズ超過で先頭から上書き。 | AIX2, AIX73 |
    | 1930 | alog -L | 登録済みログ種別 (boot / console / nim / cfg 等) と対応ファイル / サイズ / 説明を一覧表示。どの alog 種別が有効か確認する。 | AIX2, AIX73 |
    | 1931 | alog -t <type> | ログ種別指定。-t boot で起動ログ、-t console でコンソール、-t nim で NIM、-t cfg で cfgmgr 系。 | AIX2, AIX73 |
    | 1932 | alog -o | ログを表示。alog -t boot -o で boot ログ全文出力。トラブル時の基本コマンド。 | AIX2, AIX73 |
    | 1933 | alog -p <cmd> | コマンド出力を alog にパイプ書き込み。例 echo "event" \| alog -t boot -p cat。プログラムから alog に書く側。 | AIX2, AIX73 |
    | 1934 | alog -s/-C | -s でサイズ変更 (バイト、既定 boot は約 16KB)、-C で属性を ODM 登録 (永続化)。組合せで再起動後も維持。 | AIX73 |
    | 1935 | alog -t boot 確認 | 起動時のメッセージ全文。LED コード遷移、cfgmgr 出力、rc.boot 各 phase の処理が記録。boot 失敗解析の出発点。 | AIX2, AIX73 |
    | 1936 | alog -t console | コンソールに表示されたシステムメッセージ全文。errpt と二系統で取得することで取りこぼし防止。 | AIX2, AIX73 |
    | 1937 | alog -t nim | NIM (Network Installation Manager) クライアント側のインストール/復元動作ログ。 | AIX2, AIX73 |
    | 1938 | alog -t cfg | cfgmgr 起動時のデバイス検出ログ。新規 LUN マッピング後の cfgmgr -v 結果も残る。 | AIX73 |

??? note "ras （2 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1939 | /etc/ras/ | Reliability/Availability/Serviceability 関連設定の格納ディレクトリ。conscfg や ras 関連設定。 | AIX73 |
    | 1940 | /etc/ras/conscfg | console 設定。低レベル console デバイスの選択 (lft / tty / hmc 系)。chcons コマンドから操作。 | AIX73 |

??? note "sysdump （15 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1941 | sysdumpdev | システムダンプデバイス設定/状態確認コマンド。primary / secondary / autoStart / always Allow Dump / dump copy directory などを管理。 | AIX2, AIX73 |
    | 1942 | sysdumpdev -l | 現在の dump 設定を表示。primary device, secondary device, copy directory, forced copy flag, always allow dump 等が一覧で出る。 | AIX2, AIX73 |
    | 1943 | sysdumpdev -p <dev> | Primary dump device を設定。例 sysdumpdev -p /dev/lg_dumplv。次回再起動までの一時設定 (永続化には -P 併用)。 | AIX2, AIX73 |
    | 1944 | sysdumpdev -s <dev> | Secondary dump device を設定。primary が容量不足/書込不能時のフォールバック。 | AIX2, AIX73 |
    | 1945 | sysdumpdev -P | 永続化フラグ。-p / -s と併用して /etc/swapspaces や ODM (CuAt) に書き込み、再起動後も維持。 | AIX2, AIX73 |
    | 1946 | sysdumpdev -e | 次回 dump に必要な見積もりサイズをバイト表示。dump device サイズと比較して不足なら拡張要。 | AIX2, AIX73 |
    | 1947 | sysdumpdev -d <dir> | boot 時に dump を自動コピーする先ディレクトリを設定。コピー失敗時はオリジナル dump を破棄。 | AIX2, AIX73 |
    | 1948 | sysdumpdev -D <dir> | -d と同じ自動コピー指定。ただしコピー失敗時はオリジナル dump を保持。-D を推奨。 | AIX2, AIX73 |
    | 1949 | sysdumpdev -L | 最新の dump 情報 (発生日時 / dump device / 推定サイズ / 終了コード) を表示。dump 取得後の最初の確認。 | AIX2, AIX73 |
    | 1950 | sysdumpdev -K | Always Allow Dump (キー操作で dump 強制取得) を有効化。HMC の Dump キー押下に応答するか。 | AIX2, AIX73 |
    | 1951 | sysdumpdev -Z | 圧縮 dump (Compressed) サイズと dump device 容量を比較表示。 | AIX73 |
    | 1952 | sysdumpstart | コマンドからシステムダンプを強制取得。-p / -s で primary/secondary 指定。実行直後にシステムは halt → ipl。本番では計画停止時のみ。 | AIX2, AIX73 |
    | 1953 | sysdumpstart -p | Primary dump device に dump を書いて再起動。 | AIX2, AIX73 |
    | 1954 | sysdumpstart -s | Secondary dump device に dump を書いて再起動。 | AIX2, AIX73 |
    | 1955 | /dev/lg_dumplv | AIX が既定で作る dump 用 LV (rootvg 上の Logical Volume)。種別 sysdump。サイズはメモリと相関するので増設後は要拡張。 | AIX73 |

??? note "snap （11 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1956 | snap | サポート診断バンドル作成コマンド。errpt/lsattr/lsdev/lvm/network 等を一括収集し /tmp/ibmsupt 配下に保存。IBM サポート送付の標準入力物。 | AIX2, AIX73 |
    | 1957 | snap -a | All。全コンポーネント (general/kernel/lvm/nfs/printer/sna/ssa/tcpip/...) を収集。情報が多くて時間と空き容量を要する。 | AIX2, AIX73 |
    | 1958 | snap -c | 収集後にバンドルを圧縮 (pax + compress) して snap.pax.Z を作成。サポート送付前提なら通常 -c を付ける。 | AIX2, AIX73 |
    | 1959 | snap -g | General 情報のみ収集。/etc/inittab、lslpp -L、lsps -a、df、lsattr、bootlist、errpt -a など。 | AIX2, AIX73 |
    | 1960 | snap -k / -L / -n / -t / -i | 個別コンポーネント収集: -k Kernel (パラメタ/locked memory)、-L LVM (lspv/lsvg/lslv)、-n NFS (nfsstat/exports)、-t TCP/IP (netstat/ifconfig/no -a)、-i Installation。組み合わせ可。 | AIX2, AIX73 |
    | 1961 | snap -d <dir> | 出力ディレクトリ指定。既定 /tmp/ibmsupt。空きが少ない時は別 FS を指定。 | AIX2, AIX73 |
    | 1962 | snap -o / -r | -o <dev> でテープ等デバイスへ直接書き出し (例 snap -o /dev/rmt0)。-r で /tmp/ibmsupt 配下の前回 snap 出力を削除。 | AIX2, AIX73 |
    | 1963 | snap -gc | general + 圧縮 を 1 発で。日常トラブル送付の最短コマンド。 | AIX2, AIX73 |
    | 1964 | snap -e | PowerHA SystemMirror (旧 HACMP) バンドル収集。/var/hacmp/log 配下や clstrmgr 構成情報をまとめる。クラスタ障害解析の基本。 | PHA7, AIX73 |
    | 1965 | /tmp/ibmsupt | snap の出力先ディレクトリ。各コンポーネント (general/lvm/tcpip/nfs/...) 別サブディレクトリと、-c 指定時の snap.pax.Z。 | AIX2, AIX73 |
    | 1966 | snap.pax.Z | snap -c で生成される圧縮アーカイブ。IBM サポートにアップロードする実体。 | AIX2, AIX73 |

??? note "kdb （8 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1967 | kdb | AIX カーネルデバッガ。生システムでは BOS インストール時に Allow Online Diagnostics で有効化。dump 解析モードでも使う。 | AIX2, AIX73 |
    | 1968 | kdb -i | Inline モード起動 (実行中システム)。専門オペレータ向け。 | AIX73 |
    | 1969 | kdb -s <dumpfile> | System dump を読み込み解析。例 kdb /var/adm/ras/vmcore.0。プロセスリスト/スタックトレース/カーネルメモリを調査。 | AIX2, AIX73 |
    | 1970 | kdb サブコマンド | status (全 CPU 状態 / 実行中スレッド)、stack (指定スレッドの kernel stack)、print proc (プロセスエントリ表示) など。dump 解析の基本サブコマンド群。 | AIX73 |
    | 1971 | dbx | ユーザプロセス用シンボリックデバッガ。core ファイル解析や生プロセス attach。 | AIX2, AIX73 |
    | 1972 | dbx <prog> <core> | プログラムバイナリと core を指定して core dump 解析。where (スタックトレース)、print 変数、registers 等のサブコマンド。 | AIX2, AIX73 |
    | 1973 | dbx -c <cmdfile> | 起動時に実行するコマンド列をファイルから読む。バッチ解析用。 | AIX2, AIX73 |
    | 1974 | dbx -a <pid> | 実行中プロセスに attach。 | AIX73 |

??? note "trace （13 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1975 | trace | AIX kernel/user 動作トレース機構。/usr/bin/trace。イベントベースで kernel に hook を入れ trcrpt で解読。性能解析や hang 解析の決定打。 | AIX2, AIX73 |
    | 1976 | trace -a | Asynchronous 起動。trace コマンドはバックグラウンドに回り、ユーザは別シェルで操作可能。trcstop で停止。 | AIX2, AIX73 |
    | 1977 | trace -j <HookID> | 特定イベント ID だけ収集。-j 254,256 のようにカンマ区切り。 | AIX2, AIX73 |
    | 1978 | trace -k <HookID> | 指定イベント ID を除外。-j の逆。 | AIX2, AIX73 |
    | 1979 | trace -l / -d | -l Long-mode (バッファ越え時にファイル書き続け、長期キャプチャ)。-d Deferred mode (trcon が来るまで収集を遅延)。 | AIX2, AIX73 |
    | 1980 | trace -o <file> | トレース raw ファイルパス指定。既定 /var/adm/ras/trcfile。 | AIX2, AIX73 |
    | 1981 | trcstop | 実行中の trace を停止。バッファをファイルに flush。 | AIX2, AIX73 |
    | 1982 | trcon / trcoff | deferred 起動した trace の収集 on / off 切替。 | AIX2, AIX73 |
    | 1983 | trcrpt | trace raw ファイルを人間可読レポートに変換。trcrpt -O ファイル/抽出条件 を指定。 | AIX2, AIX73 |
    | 1984 | trcrpt -O "option=value" | 出力オプション指定。exec=on (exec 詳細)、svc=on (system call 詳細)、pid=on (PID 表示)、tid=on (TID 表示) など。 | AIX2, AIX73 |
    | 1985 | /var/adm/ras/trcfile | trace 既定出力ファイル。バイナリ。 | AIX2, AIX73 |
    | 1986 | iotrace | Block I/O トレース。LVM デバイスドライバ層の I/O を可視化。物理 I/O 経路の遅延解析。 | AIX73 |
    | 1987 | iptrace との違い | iotrace は block I/O 層 (FS/LVM/SCSI ドライバ)、iptrace は IP パケット。混同しないこと。 | — |

??? note "net_trace （13 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 1988 | iptrace | AIX 同梱のパケットキャプチャ。startsrc -s iptrace -a "-a -i en0 /tmp/ipt.out" のように SRC 管理で起動するのが推奨。停止は stopsrc -s iptrace。 | AIX2, AIX73 |
    | 1989 | iptrace -a | ARP パケットを除外。雑音減らし。 | AIX2, AIX73 |
    | 1990 | iptrace -i <if> | 対象インタフェース指定。例 -i en0。 | AIX2, AIX73 |
    | 1991 | iptrace -p <port> | ポート絞り込み。例 -p 80。 | AIX2, AIX73 |
    | 1992 | iptrace -s/-d <host> | 送信元/宛先ホスト絞り込み。-b で Bi-directional (逆方向も捕捉)。-P で proto (tcp/udp/icmp) 指定。 | AIX2, AIX73 |
    | 1993 | ipreport | iptrace raw を人間可読形式に整形。ipreport -ns で名前解決抑止しつつ short form。 | AIX2, AIX73 |
    | 1994 | ipreport -s / -n / -r / -x | -s 短縮、-n 名前解決抑止、-r RPC 詳細、-x hex dump。 | AIX2, AIX73 |
    | 1995 | tcpdump | AIX 同梱の標準 BPF キャプチャ。pcap 形式で書き出すため Wireshark で読める。トラブル時に iptrace より使い勝手が良い。 | AIX73 |
    | 1996 | tcpdump -i <if> | インタフェース指定。-i any で全インタフェース。 | AIX73 |
    | 1997 | tcpdump -w <file> | raw pcap ファイルに保存。後で -r で読む。 | AIX73 |
    | 1998 | tcpdump -r <file> | 保存済み pcap を読む。 | AIX73 |
    | 1999 | tcpdump -n / -nn / -s 0 | -n 名前解決抑止、-nn ポート/サービス名解決も抑止、-s 0 snap length をフル (パケット全体) に。 | AIX73 |
    | 2000 | tcpdump フィルタ式 | host x / port x / src x / dst x / tcp / udp / icmp / and / or / not の BPF 構文。-v/-vv/-vvv で詳細度、-c N で N パケットで停止。 | AIX73 |

??? note "pha_concept （13 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2001 | PowerHA SystemMirror | AIX 向け HA クラスタ製品。旧称 HACMP (High Availability Cluster Multi-Processing)。7.x から CAA (Cluster Aware AIX) 上に再構築。Standard Edition と Enterprise Edition (旧 XD) がある。 | PHA7 |
    | 2002 | Cluster | PowerHA が管理する HA 単位。1〜32 ノード (実用は 2〜8) で構成。Repository Disk (CAA) と Heartbeat ネットワークで状態同期。 | PHA7 |
    | 2003 | Node | クラスタ参加サーバ (LPAR)。各ノードは PowerHA 同一バージョン + 同一 OS レベルを推奨。clmgr add node で追加。 | PHA7 |
    | 2004 | Resource Group (RG) | 切替単位。Service IP / Volume Group / File System / Application Controller などをまとめ、1 ノードで稼動させる。フェイルオーバ時に別ノードへ移る。 | PHA7 |
    | 2005 | Service IP Label | クライアントから見える可動 IP。RG に紐付き、所属ノードのインタフェースにエイリアス追加される。フェイルオーバで別ノードへ移動。 | PHA7 |
    | 2006 | Boot IP / Persistent IP | Boot IP は起動時に持つ固定 IP (Heartbeat に使う)。Persistent IP は管理用にノード固定で常時持つ可動 IP。Service IP とは別概念。 | PHA7 |
    | 2007 | Application Controller | RG が起動/停止すべきアプリの start script / stop script を定義するオブジェクト。PowerHA 6 以前の Application Server に相当。 | PHA7 |
    | 2008 | Shared Volume Group | 複数ノードで物理ボリュームを共有する VG。Enhanced Concurrent VG (ECVG) として作成し、PowerHA がフェイルオーバ時に varyonvg を切替。 | PHA7 |
    | 2009 | Concurrent VG | 全ノードで同時に varyonvg されている VG。Oracle RAC など並列 DB 向け。RG の Online on All Available Nodes モードで使う。 | PHA7 |
    | 2010 | Shared File System | RG に紐付く JFS2 FS。フェイルオーバ時に旧ノードでアンマウント → 新ノードでマウント。Cross-mount は GPFS/Spectrum Scale 領域。 | PHA7 |
    | 2011 | RG ポリシー | Startup / Fallover / Fallback の 3 つのポリシー。例 Startup=Online On Home Node Only、Fallover=Fallover To Next Priority Node、Fallback=Never Fallback。 | PHA7 |
    | 2012 | Site | Enterprise Edition で複数サイト間 (Primary / Secondary) クラスタを構成。同期/非同期レプリで距離耐性。 | PHA7 |
    | 2013 | Topology | クラスタ構成 (ノード/ネットワーク/インタフェース/RG/サービス IP) 全体の構成情報。HACMPcluster ODM クラスに格納。 | PHA7 |

??? note "pha_daemon （10 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2014 | clstrmgrES | Cluster Manager デーモン。SRC subsystem。状態機械でノード/RG イベントを駆動。startsrc -s clstrmgrES。PowerHA の中心。 | PHA7 |
    | 2015 | clcomd | Cluster Communication Daemon。/etc/cluster/rhosts で認証されたノード間で TCP 通信を行いコマンド転送やイベント同期。 | PHA7 |
    | 2016 | clinfoES | クラスタ情報サービス。クライアントからクラスタ状態を SNMP で問合せ可能にする (旧式)。clinfo API。 | PHA7 |
    | 2017 | clevmgrdES | Event Manager デーモン。イベントスクリプト起動を制御。 | PHA7 |
    | 2018 | CAA (Cluster Aware AIX) | AIX 6.1 TL6 以降の OS 機能。PowerHA 7.x は CAA に依存。clcomd 不要のメッセージング、Repository Disk 管理、ノード メンバシップ管理を OS 層で提供。 | PHA7, AIX73 |
    | 2019 | Repository Disk | CAA が使う共有ディスク (32MB 以上、推奨 1GB)。クラスタ構成と member 情報の単一情報源。喪失するとクラスタ機能停止。 | PHA7, AIX73 |
    | 2020 | Heartbeat - IP (Unicast) | PowerHA 7.1.3 以降の既定。CAA がノード間 unicast UDP で生死確認。Multicast 不可ネットワークでも動く。 | PHA7 |
    | 2021 | Heartbeat - IP (Multicast) | PowerHA 7.1.x 初期既定。Multicast address でノード生死確認。スイッチで IGMP 設定が必要。 | PHA7 |
    | 2022 | Heartbeat - Disk (sfwcom) | SAN 経由 disk heartbeat。Repository Disk への I/O が IP ハートビートとは別経路の生死確認に。 | PHA7 |
    | 2023 | Split brain | Heartbeat 全途絶で双方が "自分が生存" と判断する状態。Repository Disk の reservation や Site policy (Quorum) で防止。 | PHA7 |

??? note "pha_clcmd （11 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2024 | cl_ コマンドファミリ | /usr/es/sbin/cluster/utilities/ や /usr/es/sbin/cluster/sbin/ 配下のクラスタ操作用シェル/バイナリ群。clmgr 登場前 (HACMP 5.x/6.x) の主力 CLI。一部は clmgr 内部から呼ばれる。 | PHA7 |
    | 2025 | cl_clstart | クラスタサービス起動 (旧形式)。実体は clstart のラッパで、引数を整え rc.cluster を起動。 | PHA7 |
    | 2026 | cl_clstop | クラスタサービス停止 (旧形式)。-N (即時), -gr (graceful release), -gw (graceful with takeover) を選択。 | PHA7 |
    | 2027 | cl_lsndvcl | ノードのデバイス情報リスト (cluster 内ノードの NIC/Disk 等)。 | PHA7 |
    | 2028 | cl_lsfs | クラスタ管理下の共有 FS 一覧。 | PHA7 |
    | 2029 | cl_lsvg | クラスタ管理下の共有 VG 一覧。lsvg のラッパで全ノード横断。 | PHA7 |
    | 2030 | cl_extconn | 拡張接続 (Extended Connectivity) チェック。ノード間ネットワーク到達性確認。 | PHA7 |
    | 2031 | cl_rsh | クラスタ間リモートシェル。clcomd 経由でリモートノードのコマンドを実行。rhosts ベースの認証。 | PHA7 |
    | 2032 | cl_rcp | クラスタ間リモートファイル転送。 | PHA7 |
    | 2033 | cl_log | PowerHA ログヘルパ。/var/hacmp/log 配下に統一形式でメッセージ書き込み。イベントスクリプトから利用。 | PHA7 |
    | 2034 | cl_echo | PowerHA メッセージカタログ参照付き echo。多言語対応のためメッセージ ID 経由でメッセージ出力。 | PHA7 |

??? note "pha_clmgr （25 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2035 | clmgr | PowerHA 7.x の統合 CLI。/usr/es/sbin/cluster/utilities/clmgr。<verb> <class> <attr=value> の構文。SMIT メニュー全機能を網羅。スクリプト自動化の標準。 | PHA7 |
    | 2036 | clmgr add cluster | 新規クラスタ作成。例 clmgr add cluster ha_cl1 REPOSITORY=hdisk5 NODES=node1,node2 TYPE=NSC。Repository Disk と参加ノードを指定。 | PHA7 |
    | 2037 | clmgr add node | 既存クラスタへノード追加。clmgr add node node3 INTERFACES=en0。 | PHA7 |
    | 2038 | clmgr add network / interface | PowerHA ネットワーク (clmgr add network net_ether_01 TYPE=ether) とインタフェース (clmgr add interface en0 NETWORK=net_ether_01 NODE=node1) を追加。 | PHA7 |
    | 2039 | clmgr add resource_group | RG 作成。clmgr add resource_group rg1 NODES=node1,node2 STARTUP=OHN FALLOVER=FNPN FALLBACK=NFB。 | PHA7 |
    | 2040 | clmgr add service_ip | Service IP Label 追加 (/etc/hosts に事前定義要)。clmgr add service_ip svc_ip1 NETWORK=net_ether_01。 | PHA7 |
    | 2041 | clmgr add application_controller | アプリケーション コントローラ追加。clmgr add application_controller app1 STARTSCRIPT=/usr/local/ha/start.sh STOPSCRIPT=/usr/local/ha/stop.sh。 | PHA7 |
    | 2042 | clmgr add volume_group / file_system | 共有 VG/FS 登録。clmgr add volume_group sharedvg1 PHYSICAL_VOLUMES=hdisk2,hdisk3 NODES=node1,node2 TYPE=enhanced_concurrent。clmgr add file_system /db1 VOLUME_GROUP=sharedvg1 SIZE=10G。 | PHA7 |
    | 2043 | clmgr add persistent_ip | Persistent IP 追加。管理用に各ノードで常時持つ。 | PHA7 |
    | 2044 | clmgr modify <class> | 属性変更系: cluster (HEARTBEAT_TYPE / REPOSITORY 等、変更後 sync 要)、resource_group (NODES 順 / STARTUP/FALLOVER/FALLBACK ポリシー)、application_controller (start/stop script / 監視設定)。 | PHA7 |
    | 2045 | clmgr delete <class> | 削除系: cluster (Repository Disk もクリア)、node、resource_group (事前 offline 化必要)、service_ip など。 | PHA7 |
    | 2046 | clmgr query cluster | クラスタ全体構成を表示。状態 (STABLE / UNSTABLE / RECONFIG) も出る。 | PHA7 |
    | 2047 | clmgr query node / resource_group | ノード一覧と状態 (UP / DOWN)、RG 一覧と現在のノード/状態 (ONLINE / OFFLINE / ERROR)。 | PHA7 |
    | 2048 | clmgr query service_ip / network / interface | Service IP 一覧 (紐付き RG 付)、PowerHA ネットワーク一覧、クラスタ管理下インタフェース一覧。 | PHA7 |
    | 2049 | clmgr query repository_disk | Repository Disk 状態。 | PHA7 |
    | 2050 | clmgr query volume_group / file_system | 管理対象 VG/FS 一覧。 | PHA7 |
    | 2051 | clmgr verify cluster | クラスタ構成を全ノード横断で検証。/etc/hosts、VG、Service IP、Application Controller などの整合性を見る。sync 前に必須。 | PHA7 |
    | 2052 | clmgr verify cluster FIX=yes / CHANGES_ONLY=yes | FIX=yes で解消可能な不整合を自動修正。CHANGES_ONLY=yes で前回 verify からの差分のみ検査して時間短縮。 | PHA7 |
    | 2053 | clmgr sync cluster | 構成変更を全ノードに伝播。verify + propagate + DARE を統合実行。HACMP 構成変更後の最終ステップ。 | PHA7 |
    | 2054 | clmgr start cluster | クラスタ全ノードのサービス起動 (rc.cluster 経由)。MANAGE=auto/manual を選べる。 | PHA7 |
    | 2055 | clmgr stop cluster | クラスタ停止。MANAGE=offline (RG 停止)、unmanage (RG 維持)、move (takeover) のいずれか。 | PHA7 |
    | 2056 | clmgr stop cluster MANAGE=unmanage | RG はそのままに clstrmgrES だけ停止 (旧 -N -gr 相当)。アプリ無停止のメンテに使う。 | PHA7 |
    | 2057 | clmgr stop cluster MANAGE=move | 停止前に RG を別ノードへ takeover (旧 -gw 相当)。 | PHA7 |
    | 2058 | clmgr online / offline resource_group | RG オンライン化: clmgr online resource_group rg1 NODE=node1。オフライン化: clmgr offline resource_group rg1 (アプリ停止のため事前合意必要)。 | PHA7 |
    | 2059 | clmgr move resource_group | RG を別ノードに手動フェイルオーバ。clmgr move resource_group rg1 NODE=node2。計画切替の標準。 | PHA7 |

??? note "pha_legacy （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2060 | HACMP | PowerHA SystemMirror の旧称 (High Availability Cluster Multi-Processing)。バージョン 5.x / 6.x で広く使われた呼称。CLI 名やパスに名残が残る (HACMPcluster ODM 等)。 | PHA7 |
    | 2061 | clstart -y | 旧 HACMP のクラスタ起動コマンド。-y で確認スキップ。現在は clmgr start cluster を推奨。 | PHA7 |
    | 2062 | clstop -y -N / -gr / -gw | -N: Now (即時) 停止 (clmgr stop cluster MANAGE=offline 相当)。-gr: Graceful (RG オフライン化後に clstrmgrES 停止)。-gw: Graceful With Takeover (RG を別ノードへ takeover してから停止、clmgr MANAGE=move 相当)。 | PHA7 |
    | 2063 | clRGmove | RG を別ノードへ移動 (旧)。clmgr move resource_group に統合された。/usr/es/sbin/cluster/utilities/clRGmove。 | PHA7 |
    | 2064 | smit hacmp | PowerHA SMIT メニュー (HACMP 由来のメニュー名)。fastpath で構成/起動/モニタを操作。 | PHA7 |
    | 2065 | HACMPcluster ODM | クラスタ構成を保持する ODM クラス群 (HACMPcluster, HACMPnode, HACMPnetwork, HACMPadapter, HACMPgroup, HACMPresource ...)。/etc/objrepos/HACMP* に格納。 | PHA7 |

??? note "pha_script （7 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2066 | /usr/es/sbin/cluster/ | PowerHA インストールルート。配下に utilities/ (CLI)、sbin/ (デーモン)、events/ (イベントスクリプト)、diag/ (診断)、samples/、cspoc/ が並ぶ。 | PHA7 |
    | 2067 | /usr/es/sbin/cluster/utilities/ | cl_ コマンド / clmgr / clRGinfo / clshowsrv の置き場。スクリプト自動化で PATH に入れる対象。 | PHA7 |
    | 2068 | /usr/es/sbin/cluster/events/ | node_up / node_down / network_up / network_down / acquire_service_addr 等の標準イベントスクリプト。クラスタ動作の核。原則編集禁止。 | PHA7 |
    | 2069 | /usr/es/sbin/cluster/diag/ / samples/ | diag/: 診断スクリプト (cldiag 系、clcheck_server / cldiag healthcheck 等)。samples/: アプリ start/stop, custom monitor, snap 拡張のサンプル (社内 application controller の雛形)。 | PHA7 |
    | 2070 | /usr/es/sbin/cluster/cspoc/ | C-SPOC (Cluster Single Point Of Control) ユーティリティ群。共有 VG 操作 / ユーザ管理を全ノードへ伝播。 | PHA7 |
    | 2071 | rc.cluster | /usr/es/sbin/cluster/etc/rc.cluster。boot 時または手動でクラスタ subsystem を順次起動するシェル。 | PHA7 |
    | 2072 | /etc/cluster/rhosts | clcomd が信頼するホスト一覧。Boot IP を全部書く。書式ミスで clcomd auth エラー → クラスタ起動不能。 | PHA7 |

??? note "pha_util （11 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2073 | clRGinfo | RG 状態確認コマンド。RG ごとの ノード/状態 (ONLINE / OFFLINE / ERROR / UNMANAGED) を一覧表示。トラブル時の第一手。 | PHA7 |
    | 2074 | clRGinfo -v | Verbose。RG のリソース内訳 (Service IP / VG / Application Controller) も表示。 | PHA7 |
    | 2075 | clRGinfo -p / -a / -m | -p ノード優先順 (Startup 順) も表示、-a 全情報 (RG + リソース + ノード優先 + ポリシー)、-m RG 移動可否判定。 | PHA7 |
    | 2076 | clshowsrv | PowerHA 関連 SRC subsystem の状態確認。clstrmgrES / clcomd / clinfoES / clevmgrdES 等。 | PHA7 |
    | 2077 | clshowsrv -a | 全 PowerHA subsystem 状態 (active / inoperative)。 | PHA7 |
    | 2078 | clshowsrv -v | Verbose。各 subsystem の起動オプションも。 | PHA7 |
    | 2079 | clRGmove (utility) | /usr/es/sbin/cluster/utilities/clRGmove。clmgr move resource_group の実装本体。手動 takeover や Sticky 操作の引数を直接扱う低レベル CLI。 | PHA7 |
    | 2080 | cldump / clstat | cldump: クラスタ状態ダンプ (clstrmgrES の状態とトポロジを 1 画面表示)。clstat: 古い ASCII ステータスモニタ。 | PHA7 |
    | 2081 | lssrc -ls clstrmgrES | clstrmgrES の詳細状態。ST_INIT / ST_STABLE / ST_RP_RUNNING 等の state machine 状態が出る。トラブルシュート定番。 | PHA7 |
    | 2082 | cltopinfo | クラスタ topology を整形表示。サイト/ノード/ネットワーク/インタフェース を階層で。 | PHA7 |
    | 2083 | clcmd | 全ノード横断コマンド実行。clcmd date のように使い、全ノード結果を統合表示。CAA 機能。 | PHA7, AIX73 |

??? note "pha_event （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2084 | Custom Pre-Event Script | 標準イベント (node_up 等) の実行前に呼ばれるユーザ定義スクリプト。clmgr add custom_event 等で登録。事前準備処理を入れる。 | PHA7 |
    | 2085 | Custom Post-Event Script | 標準イベント実行後の後処理。アプリ起動確認/通知メールなど。失敗してもイベント自体は完走する点に注意。 | PHA7 |
    | 2086 | Notify / Recovery Command | Notify: イベント開始/終了時に呼ばれる通知用コマンド (メール送信や SNMP trap)。Recovery: イベントが non-zero で失敗した時の回復処理 (Retry 回数指定可)。 | PHA7 |
    | 2087 | Application Monitor | アプリ生存監視。Process Monitor (プロセス名/PID) または Custom Monitor (任意スクリプト)。失敗時に notify / restart / fallover 動作を選べる。 | PHA7 |
    | 2088 | Custom Resource | ユーザ定義リソース。Custom Resource Type を作って RG に追加することで独自リソース (例 NFS マウント、特殊デバイス) を HA 対象にできる。 | PHA7 |
    | 2089 | Custom Verification Method | verify 時に呼ばれるユーザ検証スクリプト。社内独自要件 (例 隣接 DB 接続性) を verify cluster に組込める。 | PHA7 |

??? note "pha_log （9 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2090 | /var/hacmp/log/ | PowerHA ログディレクトリ。hacmp.out / cluster.log / clstrmgr.debug / clutils.log / clverify/ など。トラブル時はまずここを zip して送付。 | PHA7 |
    | 2091 | /var/hacmp/log/hacmp.out | 最重要ログ。各イベントスクリプトの shell -x トレースが時系列で残る。フェイルオーバ解析の本丸。 | PHA7 |
    | 2092 | /var/hacmp/log/cluster.log | クラスタ全般のサマリログ。イベント開始/完了がコンパクトに。hacmp.out の見出しとして使う。 | PHA7 |
    | 2093 | /var/hacmp/log/clstrmgr.debug | clstrmgrES デバッグログ。state machine 遷移、メッセージ送受信。デーモン内部解析に。 | PHA7 |
    | 2094 | /var/hacmp/log/clutils.log | ユーティリティ (clmgr 等) 実行ログ。 | PHA7 |
    | 2095 | /var/hacmp/log/clverify/ | verify cluster の実行ログ。clverify.log とサブログ群。検証エラーの詳細はここ。 | PHA7 |
    | 2096 | /var/hacmp/log/cspoc.log / autoverify.log | cspoc.log: C-SPOC 操作ログ。autoverify.log: Auto Cluster Verification (日次自動 verify) のログ。 | PHA7 |
    | 2097 | /var/hacmp/clcomd/ | clcomd 関連ログ (clcomd.log, clcomddiag.log)。clcomd auth エラーやノード間通信不可の調査に。 | PHA7 |
    | 2098 | /var/hacmp/log/clmgr.log | clmgr 実行ログ。 | PHA7 |

??? note "pha_snap （4 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2099 | snap -e (PowerHA bundle) | PowerHA SystemMirror 専用 snap バンドル。/var/hacmp/log 全部、ODM の HACMP* クラス、cltopinfo / clRGinfo 出力等をまとめる。クラスタ障害時のサポート問合せ標準。 | PHA7 |
    | 2100 | snap -ec | -e に圧縮 -c を加える。snap.pax.Z で出力。 | PHA7 |
    | 2101 | snap -e の出力先 | /tmp/ibmsupt/hacmp/ 配下。事前に df /tmp で空き確認。容量は数百 MB 〜 GB。 | PHA7 |
    | 2102 | snap -e 実行タイミング | 障害直後、再起動前 (ログ消失防止) に取る。可能なら全ノードで同時取得して時刻ずれを後で対比。 | PHA7 |

??? note "pha_ee （6 件）"

    | 連番 | 技術項目 | 単一焦点要約 | 出典 |
    |---:|---|---|---|
    | 2103 | PowerHA SystemMirror Enterprise Edition | 旧 HACMP/XD。複数サイト間 HA 機能 (同期/非同期レプリ) を含む上位エディション。GLVM / SVC / DS8000 Metro Mirror / Global Mirror / HyperSwap 連携。 | PHA7 |
    | 2104 | GLVM (Geographic LVM) | AIX LVM を IP 経由でサイト間ミラーする仕組み。Enterprise Edition で PowerHA と組み合わせて DR 構成。同期 / 非同期 (async cache LV) を選べる。 | PHA7, AIX73 |
    | 2105 | XD (Extended Distance) | サイト間 PowerHA の旧称。Metro Mirror / Global Mirror など。Enterprise Edition に統合。 | PHA7 |
    | 2106 | Cross-site mirroring | Primary サイトの VG/FS をリモートサイトへミラー。フェイルオーバで RG が別サイトのノードに移る。RPO/RTO 要件で同期/非同期を選択。 | PHA7 |
    | 2107 | HyperSwap | Storage 側 (DS8000) のレプリと連携し、ホストから見て透過的にプライマリ↔セカンダリ Storage を切替。PowerHA EE と組み合わせ。 | PHA7 |
    | 2108 | Site policy / RG at Site | Site policy: サイト間フェイルオーバポリシー (Online On Either Site / Online On Both Sites)。Split brain 対策 (Site Quorum) と連動。RG at Site: RG が特定サイトに preferred で割り当てられる構成、サイト間 takeover で別サイトへ移動。 | PHA7 |
