# Comm Server / VTAM / TCP/IP （2/2）

**製品**: z/OS v3 ／ **本ページ件数**: 25 件 ／ **出典区分**: Redbook ingest 反映済 v3

各技術項目の表に続き、検証手順（コンソールセッション再現）と理解度チェック（4 択 + 解説）を項目ごとの折り畳みで収録しています。

## DVIPA

**本節**: 15 件（うち検証手順・確認問題 15 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 4446 | DVIPA の役割 | Dynamic VIPA、物理インタフェースに依存しない論理 IP、Sysplex 移動可能 マニュアル抜粋: SIOCSVIPA/SIOCSVIPA6 ioctl or BIND explicitly to DVIPA. If within VIPARANGE, DVIPA created dynamically. POrt=portnum (1-65535) — Applications bound to DVIPA and this port number on target stacks are paused from sysplex … (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4447 | VIPADYNAMIC ブロック | VIPADYNAMIC/ENDVIPADYNAMIC で DVIPA 関連を囲む マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4448 | VIPADEFINE | DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等) マニュアル抜粋: VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIP… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4449 | VIPABACKUP | 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ マニュアル抜粋: VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIP… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4450 | VIPARANGE | アプリ生成型 DVIPA (BIND() で起動) を許可する範囲 マニュアル抜粋: SIOCSVIPA/SIOCSVIPA6 ioctl or BIND explicitly to DVIPA. If within VIPARANGE, DVIPA created dynamically. POrt=portnum (1-65535) — Applications bound to DVIPA and this port number on target stacks are paused from sysplex … (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4451 | VIPADISTRIBUTE | Sysplex Distributor の宛先 DVIPA とターゲットスタック群を定義 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4452 | DESTIP/PORT 指定 | VIPADISTRIBUTE で配布対象ポートを限定 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4453 | SYSPLEXPORTS | Sysplex Distributor が一意エフェメラルポートを管理 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4454 | Sysplex Distributor 概要 | 1 つの DVIPA に複数ターゲットスタックを配下、WLM 重み付け配布 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4455 | SD WEIGHT/SHAREPORTWLM | WLM 重みに基づく配布、複数 listener の負荷分散 マニュアル抜粋: VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIP… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4456 | VIPA Move | 本拠地スタック停止時にバックアップへ DVIPA が移動 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4457 | VIPA Takeover/Giveback | 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK) マニュアル抜粋: VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIP… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4458 | D TCPIP,,SYSPLEX,VIPADYN | DVIPA 一覧と現在のオーナー/バックアップ表示 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4459 | D TCPIP,,SYSPLEX,PORTS | Sysplex Distributor 配下のポート別配布状況 マニュアル抜粋: Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4460 | Sysplex 監視 SYSPLEXMONITOR | GLOBALCONFIG SYSPLEXMONITOR で異常時自動退避設定 マニュアル抜粋: VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIP… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#4446 DVIPA の役割"

    **検証手順**

    検証目的: 「DVIPA の役割」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - DVIPA の役割
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [DVIPA の役割 に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      SIOCSVIPA/SIOCSVIPA6 ioctl or BIND explicitly to DVIPA. If within VIPARANGE, DVIPA created dynamically. POrt=portnum (1-65535) — Applications bound to DVIPA and this port number on target stacks are paused from sysplex distributor workload balancing.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=DVIPA の役割、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「DVIPA の役割」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「DVIPA の役割」(DVIPA) の説明として正しいものはどれか?

    - A. Dynamic VIPA、物理インタフェースに依存しない論理 IP、Sysplex 移動可能
    - B. VIPADYNAMIC/ENDVIPADYNAMIC で DVIPA 関連を囲む
    - C. DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等)
    - D. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ

    正解: **A**

    解説: 正解 (A) は「DVIPA の役割」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「DVIPA の役割」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4447 VIPADYNAMIC ブロック"

    **検証手順**

    検証目的: 「VIPADYNAMIC ブロック」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPADYNAMIC ブロック
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPADYNAMIC ブロック に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPADYNAMIC ブロック、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPADYNAMIC ブロック」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPADYNAMIC ブロック」(DVIPA) の説明として正しいものはどれか?

    - A. Dynamic VIPA、物理インタフェースに依存しない論理 IP、Sysplex 移動可能
    - B. DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等)
    - C. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ
    - D. VIPADYNAMIC/ENDVIPADYNAMIC で DVIPA 関連を囲む

    正解: **D**

    解説: 正解 (D) は「VIPADYNAMIC ブロック」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPADYNAMIC ブロック」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4448 VIPADEFINE"

    **検証手順**

    検証目的: 「VIPADEFINE」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPADEFINE
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPADEFINE に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIPA activated when VIPADEFINE is processed.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPADEFINE、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPADEFINE」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPADEFINE」(DVIPA) の説明として正しいものはどれか?

    - A. DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等)
    - B. VIPADYNAMIC/ENDVIPADYNAMIC で DVIPA 関連を囲む
    - C. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ
    - D. Dynamic VIPA、物理インタフェースに依存しない論理 IP、Sysplex 移動可能

    正解: **A**

    解説: 正解 (A) は「VIPADEFINE」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPADEFINE」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4449 VIPABACKUP"

    **検証手順**

    検証目的: 「VIPABACKUP」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPABACKUP
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPABACKUP に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIPA activated when VIPADEFINE is processed.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPABACKUP、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPABACKUP」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPABACKUP」(DVIPA) の説明として正しいものはどれか?

    - A. DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等)
    - B. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ
    - C. アプリ生成型 DVIPA (BIND() で起動) を許可する範囲
    - D. VIPADYNAMIC/ENDVIPADYNAMIC で DVIPA 関連を囲む

    正解: **B**

    解説: 正解 (B) は「VIPABACKUP」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPABACKUP」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4450 VIPARANGE"

    **検証手順**

    検証目的: 「VIPARANGE」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPARANGE
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPARANGE に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      SIOCSVIPA/SIOCSVIPA6 ioctl or BIND explicitly to DVIPA. If within VIPARANGE, DVIPA created dynamically. POrt=portnum (1-65535) — Applications bound to DVIPA and this port number on target stacks are paused from sysplex distributor workload balancing.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPARANGE、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPARANGE」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPARANGE」(DVIPA) の説明として正しいものはどれか?

    - A. アプリ生成型 DVIPA (BIND() で起動) を許可する範囲
    - B. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ
    - C. Sysplex Distributor の宛先 DVIPA とターゲットスタック群を定義
    - D. DVIPA を本拠地スタックに定義 (MOVEABLE/NONDISRUPTIVE 等)

    正解: **A**

    解説: 正解 (A) は「VIPARANGE」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPARANGE」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4451 VIPADISTRIBUTE"

    **検証手順**

    検証目的: 「VIPADISTRIBUTE」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPADISTRIBUTE
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPADISTRIBUTE に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPADISTRIBUTE、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPADISTRIBUTE」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPADISTRIBUTE」(DVIPA) の説明として正しいものはどれか?

    - A. Sysplex Distributor の宛先 DVIPA とターゲットスタック群を定義
    - B. アプリ生成型 DVIPA (BIND() で起動) を許可する範囲
    - C. VIPADISTRIBUTE で配布対象ポートを限定
    - D. 他スタックでバックアップ DVIPA を定義、Rank 順に引継ぎ

    正解: **A**

    解説: 正解 (A) は「VIPADISTRIBUTE」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPADISTRIBUTE」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4452 DESTIP/PORT 指定"

    **検証手順**

    検証目的: 「DESTIP/PORT 指定」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - DESTIP/PORT 指定
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [DESTIP/PORT 指定 に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=DESTIP/PORT 指定、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「DESTIP/PORT 指定」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「DESTIP/PORT 指定」(DVIPA) の説明として正しいものはどれか?

    - A. Sysplex Distributor の宛先 DVIPA とターゲットスタック群を定義
    - B. Sysplex Distributor が一意エフェメラルポートを管理
    - C. VIPADISTRIBUTE で配布対象ポートを限定
    - D. アプリ生成型 DVIPA (BIND() で起動) を許可する範囲

    正解: **C**

    解説: 正解 (C) は「DESTIP/PORT 指定」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「DESTIP/PORT 指定」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4453 SYSPLEXPORTS"

    **検証手順**

    検証目的: 「SYSPLEXPORTS」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - SYSPLEXPORTS
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [SYSPLEXPORTS に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=SYSPLEXPORTS、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「SYSPLEXPORTS」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「SYSPLEXPORTS」(DVIPA) の説明として正しいものはどれか?

    - A. VIPADISTRIBUTE で配布対象ポートを限定
    - B. Sysplex Distributor が一意エフェメラルポートを管理
    - C. 1 つの DVIPA に複数ターゲットスタックを配下、WLM 重み付け配布
    - D. Sysplex Distributor の宛先 DVIPA とターゲットスタック群を定義

    正解: **B**

    解説: 正解 (B) は「SYSPLEXPORTS」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「SYSPLEXPORTS」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4454 Sysplex Distributor 概要"

    **検証手順**

    検証目的: 「Sysplex Distributor 概要」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - Sysplex Distributor 概要
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [Sysplex Distributor 概要 に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=Sysplex Distributor 概要、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「Sysplex Distributor 概要」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「Sysplex Distributor 概要」(DVIPA) の説明として正しいものはどれか?

    - A. Sysplex Distributor が一意エフェメラルポートを管理
    - B. WLM 重みに基づく配布、複数 listener の負荷分散
    - C. VIPADISTRIBUTE で配布対象ポートを限定
    - D. 1 つの DVIPA に複数ターゲットスタックを配下、WLM 重み付け配布

    正解: **D**

    解説: 正解 (D) は「Sysplex Distributor 概要」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「Sysplex Distributor 概要」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4455 SD WEIGHT/SHAREPORTWLM"

    **検証手順**

    検証目的: 「SD WEIGHT/SHAREPORTWLM」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - SD WEIGHT/SHAREPORTWLM
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [SD WEIGHT/SHAREPORTWLM に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIPA activated when VIPADEFINE is processed.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=SD WEIGHT/SHAREPORTWLM、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「SD WEIGHT/SHAREPORTWLM」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「SD WEIGHT/SHAREPORTWLM」(DVIPA) の説明として正しいものはどれか?

    - A. WLM 重みに基づく配布、複数 listener の負荷分散
    - B. 1 つの DVIPA に複数ターゲットスタックを配下、WLM 重み付け配布
    - C. 本拠地スタック停止時にバックアップへ DVIPA が移動
    - D. Sysplex Distributor が一意エフェメラルポートを管理

    正解: **A**

    解説: 正解 (A) は「SD WEIGHT/SHAREPORTWLM」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「SD WEIGHT/SHAREPORTWLM」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4456 VIPA Move"

    **検証手順**

    検証目的: 「VIPA Move」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPA Move
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPA Move に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPA Move、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPA Move」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPA Move」(DVIPA) の説明として正しいものはどれか?

    - A. WLM 重みに基づく配布、複数 listener の負荷分散
    - B. 本拠地スタック停止時にバックアップへ DVIPA が移動
    - C. 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK)
    - D. 1 つの DVIPA に複数ターゲットスタックを配下、WLM 重み付け配布

    正解: **B**

    解説: 正解 (B) は「VIPA Move」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPA Move」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4457 VIPA Takeover/Giveback"

    **検証手順**

    検証目的: 「VIPA Takeover/Giveback」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - VIPA Takeover/Giveback
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [VIPA Takeover/Giveback に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIPA activated when VIPADEFINE is processed.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VIPA Takeover/Giveback、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VIPA Takeover/Giveback」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VIPA Takeover/Giveback」(DVIPA) の説明として正しいものはどれか?

    - A. 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK)
    - B. 本拠地スタック停止時にバックアップへ DVIPA が移動
    - C. DVIPA 一覧と現在のオーナー/バックアップ表示
    - D. WLM 重みに基づく配布、複数 listener の負荷分散

    正解: **A**

    解説: 正解 (A) は「VIPA Takeover/Giveback」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「VIPA Takeover/Giveback」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4458 D TCPIP,,SYSPLEX,VIPADYN"

    **検証手順**

    検証目的: 「D TCPIP,,SYSPLEX,VIPADYN」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - D TCPIP,,SYSPLEX,VIPADYN
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [D TCPIP,,SYSPLEX,VIPADYN に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=D TCPIP,,SYSPLEX,VIPADYN、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「D TCPIP,,SYSPLEX,VIPADYN」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「D TCPIP,,SYSPLEX,VIPADYN」(DVIPA) の説明として正しいものはどれか?

    - A. 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK)
    - B. Sysplex Distributor 配下のポート別配布状況
    - C. DVIPA 一覧と現在のオーナー/バックアップ表示
    - D. 本拠地スタック停止時にバックアップへ DVIPA が移動

    正解: **C**

    解説: 正解 (C) は「D TCPIP,,SYSPLEX,VIPADYN」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「D TCPIP,,SYSPLEX,VIPADYN」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4459 D TCPIP,,SYSPLEX,PORTS"

    **検証手順**

    検証目的: 「D TCPIP,,SYSPLEX,PORTS」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - D TCPIP,,SYSPLEX,PORTS
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [D TCPIP,,SYSPLEX,PORTS に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      Sysplex distributor: VIPADISTRIBUTE statement can designate up to 32 stacks and 64 ports where connections for a particular DVIPA can be distributed. DVIPA must be ACTIVE status.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=D TCPIP,,SYSPLEX,PORTS、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「D TCPIP,,SYSPLEX,PORTS」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「D TCPIP,,SYSPLEX,PORTS」(DVIPA) の説明として正しいものはどれか?

    - A. DVIPA 一覧と現在のオーナー/バックアップ表示
    - B. GLOBALCONFIG SYSPLEXMONITOR で異常時自動退避設定
    - C. Sysplex Distributor 配下のポート別配布状況
    - D. 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK)

    正解: **C**

    解説: 正解 (C) は「D TCPIP,,SYSPLEX,PORTS」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「D TCPIP,,SYSPLEX,PORTS」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4460 Sysplex 監視 SYSPLEXMONITOR"

    **検証手順**

    検証目的: 「Sysplex 監視 SYSPLEXMONITOR」(DVIPA) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    TSO ISPF EDIT — PROFILE.TCPIP の VIPADYNAMIC ブロック編集

    [コマンド 1] PROFILE.TCPIP の VIPADYNAMIC ブロックを ISPF EDIT で開く
      EDIT 'TCPIP.PROFILE(TCPIP)'    (VIPADYNAMIC ブロック)

    [オプション説明]
      DVIPA            - Dynamic VIPA (動的 Virtual IP Address)
      VIPADEFINE       - 主 DVIPA 定義
      VIPABACKUP       - バックアップ DVIPA 定義
      VIPADISTRIBUTE   - Sysplex Distributor 配信先
      対象             - Sysplex 監視 SYSPLEXMONITOR
      (出典: z/OS Communications Server IP Configuration Reference (DVIPA章)、原文ママ)

    [出力 1]
      VIPADYNAMIC
        VIPADEFINE [MOVEABLE|...] 255.255.255.0 [DVIPA アドレス]
        VIPABACKUP [...] [DVIPA アドレス]
        VIPADISTRIBUTE [DVIPA] PORT [...] DESTIP [...]
        [Sysplex 監視 SYSPLEXMONITOR に関連するステートメント]
      ENDVIPADYNAMIC

    [コマンド 2] V TCPIP,,OBEYFILE で動的反映 + D TCPIP,,SYSPLEX,VIPADYN で確認
      V TCPIP,TCPIP,OBEYFILE,USER1.TCPIP(DVIPADEF)
      D TCPIP,TCPIP,SYSPLEX,VIPADYN

    [出力 2]
      EZZ0053I COMMAND VARY OBEYFILE COMPLETED SUCCESSFULLY
      EZZ8260I SYSPLEX CS V[...] [DVIPA] [状態]
      (★ 反映確認 — DVIPA が ACTIVE / MOVING / BACKUP 等の正しい状態で表示されれば成功)

    [出典抜粋]
      VIPADEFINE and VIPABACKUP — When adding a dynamic VIPA to a functioning sysplex, add VIPABACKUP statements to backup stacks first, then add VIPADEFINE statement to the TCP/IP stack where the VIPA should normally be. VIPA activated when VIPADEFINE is processed.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=Sysplex 監視 SYSPLEXMONITOR、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「Sysplex 監視 SYSPLEXMONITOR」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「Sysplex 監視 SYSPLEXMONITOR」(DVIPA) の説明として正しいものはどれか?

    - A. GLOBALCONFIG SYSPLEXMONITOR で異常時自動退避設定
    - B. Sysplex Distributor 配下のポート別配布状況
    - C. DVIPA 一覧と現在のオーナー/バックアップ表示
    - D. 計画停止/再起動時の戻し制御 (AUTOSWITCHBACK)

    正解: **A**

    解説: 正解 (A) は「Sysplex 監視 SYSPLEXMONITOR」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「DVIPA」内の隣接項目要約であり、「Sysplex 監視 SYSPLEXMONITOR」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


## Comm 補足

**本節**: 10 件（うち検証手順・確認問題 10 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 4461 | VTAM と TCP/IP の起動順 | OMVS/RESOLVER/VTAM 起動後に TCP/IP、TN3270 は TCP/IP 後 マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4462 | Stack Affinity | TCPIP.DATA TCPIPJOBNAME で複数スタック中の対象を選択 マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4463 | BPXPRMxx と Sockets | Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4464 | CTRACE TCP/IP | Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替 マニュアル抜粋: Policy Agent start options: -d n (debug level), -i (read configuration files immediately if changed), -t n (LDAP client trace), -l filename\|SYSLOGD (log destination; SYSLOGD recommended). /tmp/pagent.pid contains proces… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4465 | PKTTRACE | TCP/IP パケットトレース、フィルタを指定して CTRACE バッファへ マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4466 | DATATRACE | ソケット層データの内容トレース マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4467 | VTAM CSDUMP | 条件付きメモリダンプ起動、メッセージ ID トリガで取得 マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4468 | RACF SERVAUTH | TCP/IP ポート/ネット/スタック/サービスのアクセス制御クラス マニュアル抜粋: PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log. (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4469 | PAGENT (Policy Agent) | QoS/IDS/IPSec/TTLS/Routing ポリシーを集中管理するデーモン マニュアル抜粋: Policy Agent start options: -d n (debug level), -i (read configuration files immediately if changed), -t n (LDAP client trace), -l filename\|SYSLOGD (log destination; SYSLOGD recommended). /tmp/pagent.pid contains proces… (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |
| 4470 | syslogd | z/OS UNIX syslog 受信、TCP/IP/AT-TLS/IDS のログ転送先 マニュアル抜粋: If running Policy Agent with nonzero UID using z/OS UNIX log file: file permissions either 776 or 766. Ensure syslog daemon not configured to log the same z/OS UNIX file (syslogd UID 0 may block PAGENT). (出典: z/OS Communications Server SNA Operation、z/OS Communications Server IP Configuration Reference、原文ママ) | AS2, ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#4461 VTAM と TCP/IP の起動順"

    **検証手順**

    検証目的: 「VTAM と TCP/IP の起動順」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — VTAM と TCP/IP の起動順 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - VTAM と TCP/IP の起動順
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に VTAM と TCP/IP の起動順 関連アドレス空間が表示)
      IEE114I [時刻] [...] VTAM と TCP/IP の起動順    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — VTAM と TCP/IP の起動順 の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VTAM と TCP/IP の起動順、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VTAM と TCP/IP の起動順」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VTAM と TCP/IP の起動順」(Comm 補足) の説明として正しいものはどれか?

    - A. OMVS/RESOLVER/VTAM 起動後に TCP/IP、TN3270 は TCP/IP 後
    - B. TCPIP.DATA TCPIPJOBNAME で複数スタック中の対象を選択
    - C. Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET
    - D. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替

    正解: **A**

    解説: 正解 (A) は「VTAM と TCP/IP の起動順」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「VTAM と TCP/IP の起動順」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4462 Stack Affinity"

    **検証手順**

    検証目的: 「Stack Affinity」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — Stack Affinity 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - Stack Affinity
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に Stack Affinity 関連アドレス空間が表示)
      IEE114I [時刻] [...] Stack Affinity    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — Stack Affinity の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=Stack Affinity、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「Stack Affinity」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「Stack Affinity」(Comm 補足) の説明として正しいものはどれか?

    - A. OMVS/RESOLVER/VTAM 起動後に TCP/IP、TN3270 は TCP/IP 後
    - B. TCPIP.DATA TCPIPJOBNAME で複数スタック中の対象を選択
    - C. Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET
    - D. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替

    正解: **B**

    解説: 正解 (B) は「Stack Affinity」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「Stack Affinity」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4463 BPXPRMxx と Sockets"

    **検証手順**

    検証目的: 「BPXPRMxx と Sockets」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — BPXPRMxx と Sockets 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - BPXPRMxx と Sockets
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に BPXPRMxx と Sockets 関連アドレス空間が表示)
      IEE114I [時刻] [...] BPXPRMxx と Sockets    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — BPXPRMxx と Sockets の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=BPXPRMxx と Sockets、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「BPXPRMxx と Sockets」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「BPXPRMxx と Sockets」(Comm 補足) の説明として正しいものはどれか?

    - A. TCPIP.DATA TCPIPJOBNAME で複数スタック中の対象を選択
    - B. Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET
    - C. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替
    - D. OMVS/RESOLVER/VTAM 起動後に TCP/IP、TN3270 は TCP/IP 後

    正解: **B**

    解説: 正解 (B) は「BPXPRMxx と Sockets」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「BPXPRMxx と Sockets」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4464 CTRACE TCP/IP"

    **検証手順**

    検証目的: 「CTRACE TCP/IP」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — CTRACE TCP/IP 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - CTRACE TCP/IP
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に CTRACE TCP/IP 関連アドレス空間が表示)
      IEE114I [時刻] [...] CTRACE TCP/IP    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — CTRACE TCP/IP の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      Policy Agent start options: -d n (debug level), -i (read configuration files immediately if changed), -t n (LDAP client trace), -l filename|SYSLOGD (log destination; SYSLOGD recommended). /tmp/pagent.pid contains process ID.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=CTRACE TCP/IP、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「CTRACE TCP/IP」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「CTRACE TCP/IP」(Comm 補足) の説明として正しいものはどれか?

    - A. Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET
    - B. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替
    - C. TCP/IP パケットトレース、フィルタを指定して CTRACE バッファへ
    - D. TCPIP.DATA TCPIPJOBNAME で複数スタック中の対象を選択

    正解: **B**

    解説: 正解 (B) は「CTRACE TCP/IP」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「CTRACE TCP/IP」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4465 PKTTRACE"

    **検証手順**

    検証目的: 「PKTTRACE」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — PKTTRACE 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - PKTTRACE
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に PKTTRACE 関連アドレス空間が表示)
      IEE114I [時刻] [...] PKTTRACE    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — PKTTRACE の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=PKTTRACE、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「PKTTRACE」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「PKTTRACE」(Comm 補足) の説明として正しいものはどれか?

    - A. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替
    - B. ソケット層データの内容トレース
    - C. TCP/IP パケットトレース、フィルタを指定して CTRACE バッファへ
    - D. Sockets API は AF_INET/AF_INET6 サポート、SYSPLEX 共用は CINET

    正解: **C**

    解説: 正解 (C) は「PKTTRACE」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「PKTTRACE」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4466 DATATRACE"

    **検証手順**

    検証目的: 「DATATRACE」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — DATATRACE 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - DATATRACE
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に DATATRACE 関連アドレス空間が表示)
      IEE114I [時刻] [...] DATATRACE    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — DATATRACE の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=DATATRACE、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「DATATRACE」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「DATATRACE」(Comm 補足) の説明として正しいものはどれか?

    - A. ソケット層データの内容トレース
    - B. TCP/IP パケットトレース、フィルタを指定して CTRACE バッファへ
    - C. 条件付きメモリダンプ起動、メッセージ ID トリガで取得
    - D. Component Trace、TYPE=SYSTCPIP で各種オプションを動的に切替

    正解: **A**

    解説: 正解 (A) は「DATATRACE」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「DATATRACE」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4467 VTAM CSDUMP"

    **検証手順**

    検証目的: 「VTAM CSDUMP」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — VTAM CSDUMP 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - VTAM CSDUMP
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に VTAM CSDUMP 関連アドレス空間が表示)
      IEE114I [時刻] [...] VTAM CSDUMP    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — VTAM CSDUMP の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=VTAM CSDUMP、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「VTAM CSDUMP」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「VTAM CSDUMP」(Comm 補足) の説明として正しいものはどれか?

    - A. 条件付きメモリダンプ起動、メッセージ ID トリガで取得
    - B. ソケット層データの内容トレース
    - C. TCP/IP ポート/ネット/スタック/サービスのアクセス制御クラス
    - D. TCP/IP パケットトレース、フィルタを指定して CTRACE バッファへ

    正解: **A**

    解説: 正解 (A) は「VTAM CSDUMP」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「VTAM CSDUMP」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4468 RACF SERVAUTH"

    **検証手順**

    検証目的: 「RACF SERVAUTH」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — RACF SERVAUTH 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - RACF SERVAUTH
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に RACF SERVAUTH 関連アドレス空間が表示)
      IEE114I [時刻] [...] RACF SERVAUTH    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — RACF SERVAUTH の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      PAGENT_LOG_FILE_CONTROL=x,y — x is log file size (KB, max 1,000,000); y is number of log files. Default 3 files at 300KB each. Default log location /tmp/pagent.log.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=RACF SERVAUTH、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「RACF SERVAUTH」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「RACF SERVAUTH」(Comm 補足) の説明として正しいものはどれか?

    - A. 条件付きメモリダンプ起動、メッセージ ID トリガで取得
    - B. QoS/IDS/IPSec/TTLS/Routing ポリシーを集中管理するデーモン
    - C. TCP/IP ポート/ネット/スタック/サービスのアクセス制御クラス
    - D. ソケット層データの内容トレース

    正解: **C**

    解説: 正解 (C) は「RACF SERVAUTH」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「RACF SERVAUTH」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4469 PAGENT (Policy Agent)"

    **検証手順**

    検証目的: 「PAGENT (Policy Agent)」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — PAGENT (Policy Agent) 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - PAGENT (Policy Agent)
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に PAGENT (Policy Agent) 関連アドレス空間が表示)
      IEE114I [時刻] [...] PAGENT (Policy Agent)    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — PAGENT (Policy Agent) の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      Policy Agent start options: -d n (debug level), -i (read configuration files immediately if changed), -t n (LDAP client trace), -l filename|SYSLOGD (log destination; SYSLOGD recommended). /tmp/pagent.pid contains process ID.
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=PAGENT (Policy Agent)、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「PAGENT (Policy Agent)」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「PAGENT (Policy Agent)」(Comm 補足) の説明として正しいものはどれか?

    - A. QoS/IDS/IPSec/TTLS/Routing ポリシーを集中管理するデーモン
    - B. TCP/IP ポート/ネット/スタック/サービスのアクセス制御クラス
    - C. z/OS UNIX syslog 受信、TCP/IP/AT-TLS/IDS のログ転送先
    - D. 条件付きメモリダンプ起動、メッセージ ID トリガで取得

    正解: **A**

    解説: 正解 (A) は「PAGENT (Policy Agent)」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「PAGENT (Policy Agent)」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。


??? example "#4470 syslogd"

    **検証手順**

    検証目的: 「syslogd」(Comm 補足) の構文・パラメータ・運用上の役割を z/OS Communications Server マニュアルと実機で確認し、VTAM / TCP/IP オペレータが正しく定義・コマンド操作できることを検証する。

    ```text
    [セッション環境]
    z/OS マスターコンソール / TSO / USS シェル — syslogd 関連

    [コマンド 1] 該当デーモンの起動状態確認 / 設定ファイル参照
      D A,L
      D TCPIP,,N,HOME

    [オプション説明]
      対象       - syslogd
      関連デーモン - PAGENT/syslogd/RESOLVER 等
      (出典: z/OS Communications Server IP Configuration Reference / IP Programmer's Guide、原文ママ)

    [出力 1]
      (D A,L の出力に syslogd 関連アドレス空間が表示)
      IEE114I [時刻] [...] syslogd    [...]

    [コマンド 2] 該当機能のログ / 設定 / 動作を実機で検証
      (個別運用手順に従う)

    [出力 2]
      (出典マニュアル該当章の期待動作が得られれば成功)
      (★ 反映確認 — syslogd の役割が出典マニュアルどおりに機能していれば成功)

    [出典抜粋]
      If running Policy Agent with nonzero UID using z/OS UNIX log file: file permissions either 776 or 766. Ensure syslog daemon not configured to log the same z/OS UNIX file (syslogd UID 0 may block PAGENT).
      (出典: z/OS Communications Server マニュアル、原文ママ)
    ```

    前提条件: TCP/IP スタック (TCPIP プロシージャ) 稼働中、PROFILE.TCPIP 参照権限、TCP/IP オペレータ ID 権限、関連デーモン (PAGENT/syslogd/RESOLVER 等) 稼働

    プレースホルダ: [対象項目]=syslogd、[TCPIP プロシージャ名]=サイト指定 (既定 TCPIP)、[VTAMLST メンバ名]=サイト指定、[リソース名]=サイトで実在する Major Node / APPL / PU / LU 等 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS Communications Server IP Configuration Reference) の「syslogd」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (パラメータ/メンバ名/オペランド値含む)
    - ③ EZZ/EZY/EZD メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象リソース状態 (D NET / D TCPIP / NETSTAT の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS / Comm Server リリース / TCPIP・VTAM プロシージャ名 / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「syslogd」(Comm 補足) の説明として正しいものはどれか?

    - A. QoS/IDS/IPSec/TTLS/Routing ポリシーを集中管理するデーモン
    - B. TCP/IP ポート/ネット/スタック/サービスのアクセス制御クラス
    - C. z/OS UNIX syslog 受信、TCP/IP/AT-TLS/IDS のログ転送先
    - D. 条件付きメモリダンプ起動、メッセージ ID トリガで取得

    正解: **C**

    解説: 正解 (C) は「syslogd」の単一焦点要約 (出典: z/OS Communications Server マニュアル、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「Comm 補足」内の隣接項目要約であり、「syslogd」の説明としては誤り。実機 D NET / D TCPIP / NETSTAT で検証し、出典章で詳細確認推奨。
