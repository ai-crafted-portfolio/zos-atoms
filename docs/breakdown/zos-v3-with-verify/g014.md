# MVS オペレータコマンド （2/2）

**製品**: z/OS v3 ／ **本ページ件数**: 15 件 ／ **出典区分**: Redbook ingest 反映済 v3

各技術項目の表に続き、検証手順（コンソールセッション再現）と理解度チェック（4 択 + 解説）を項目ごとの折り畳みで収録しています。

## K

**本節**: 6 件（うち検証手順・確認問題 6 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 2671 | K コマンド基本 | CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2672 | K S,DEL=... | コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2673 | K M,REF メッセージ再表示 | K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2674 | K E,D 削除 | K E,D で表示メッセージを消去 (Erase) する形式。視認性を上げるための運用補助。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2675 | K N,PFK=(xx) PFK 切替 | PF キーセット (PFKTABxx の SUFFIX) を動的に切り替える。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2676 | K A,NONE ロール解除 | コンソールの ROLL 動作を停止して固定表示に切り替える。 マニュアル抜粋 (K): [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME/RNUM allowed, action messages roll off screen after RTME interval. Roll-deletable — accumulate at top of screen, operator removes them. Wrap — overlaid by new messages. Wrap mode restrictions: cannot use CONTROL A (define out-of-line display areas), CONTROL D,N,HOLD (number and hold messages), CONTROL E,nn(,nn), CONTR... (出典: z/OS MVS System Commands、原文ママ) | ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#2671 K コマンド基本"

    **検証手順**

    検証目的: 「K コマンド基本」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K コマンド基本)
    [オプション説明]
      K - K コマンド基本 を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K コマンド基本 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K コマンド基本」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K コマンド基本」(K) の説明として正しいものはどれか?

    - A. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - B. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。
    - C. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - D. K E,D で表示メッセージを消去 (Erase) する形式。視認性を上げるための運用補助。

    正解: **C**

    解説: 正解 (C) は「K コマンド基本」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K コマンド基本」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


??? example "#2672 K S,DEL=..."

    **検証手順**

    検証目的: 「K S,DEL=...」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K S,DEL=...)
    [オプション説明]
      K - K S,DEL=... を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K S,DEL=... に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K S,DEL=...」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K S,DEL=...」(K) の説明として正しいものはどれか?

    - A. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - B. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。
    - C. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - D. K E,D で表示メッセージを消去 (Erase) する形式。視認性を上げるための運用補助。

    正解: **C**

    解説: 正解 (C) は「K S,DEL=...」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K S,DEL=...」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


??? example "#2673 K M,REF メッセージ再表示"

    **検証手順**

    検証目的: 「K M,REF メッセージ再表示」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K M,REF メッセージ再表示)
    [オプション説明]
      K - K M,REF メッセージ再表示 を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K M,REF メッセージ再表示 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K M,REF メッセージ再表示」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K M,REF メッセージ再表示」(K) の説明として正しいものはどれか?

    - A. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - B. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - C. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。
    - D. K E,D で表示メッセージを消去 (Erase) する形式。視認性を上げるための運用補助。

    正解: **C**

    解説: 正解 (C) は「K M,REF メッセージ再表示」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K M,REF メッセージ再表示」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


??? example "#2674 K E,D 削除"

    **検証手順**

    検証目的: 「K E,D 削除」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K E,D 削除)
    [オプション説明]
      K - K E,D 削除 を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K E,D 削除 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K E,D 削除」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K E,D 削除」(K) の説明として正しいものはどれか?

    - A. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - B. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - C. K E,D で表示メッセージを消去 (Erase) する形式。視認性を上げるための運用補助。
    - D. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。

    正解: **C**

    解説: 正解 (C) は「K E,D 削除」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K E,D 削除」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


??? example "#2675 K N,PFK=(xx) PFK 切替"

    **検証手順**

    検証目的: 「K N,PFK=(xx) PFK 切替」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K N,PFK=(xx) PFK 切替)
    [オプション説明]
      K - K N,PFK=(xx) PFK 切替 を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K N,PFK=(xx) PFK 切替 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K N,PFK=(xx) PFK 切替」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K N,PFK=(xx) PFK 切替」(K) の説明として正しいものはどれか?

    - A. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - B. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - C. PF キーセット (PFKTABxx の SUFFIX) を動的に切り替える。
    - D. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。

    正解: **C**

    解説: 正解 (C) は「K N,PFK=(xx) PFK 切替」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K N,PFK=(xx) PFK 切替」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


??? example "#2676 K A,NONE ロール解除"

    **検証手順**

    検証目的: 「K A,NONE ロール解除」(K) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] K を投入 (運用目的: K A,NONE ロール解除)
    [オプション説明]
      K - K A,NONE ロール解除 を確認するための K 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag300] PFK defaults for console: PFK 5 assigned CONTROL S,DEL=N (stop messages from rolling). PFK 6 defined as CONTROL S,DEL=RD (place screen in roll-deletable mode, prevent message backup). Comparison of Roll, Roll-deletable, Wrap Mode and HOLDMODE: Roll — HOLDMODE/RTME...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=K A,NONE ロール解除 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「K A,NONE ロール解除」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (K を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「K A,NONE ロール解除」(K) の説明として正しいものはどれか?

    - A. CONTROL コマンドの 1 字省略形。コンソール属性の動的変更 (PFK、ROLL、RNUM、M REF 等) に用いる。
    - B. コンソールのメッセージ削除 (スクロール) モードを設定する。R/RD/N の各モードを切替。
    - C. コンソールの ROLL 動作を停止して固定表示に切り替える。
    - D. K M,REF で未応答 WTOR / アクション・メッセージを再描画する。流れ去ったメッセージの確認に使う。

    正解: **C**

    解説: 正解 (C) は「K A,NONE ロール解除」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「K」内の隣接項目要約であり、「K A,NONE ロール解除」の説明としては誤り。実機 K で検証し、出典章で詳細確認推奨。


## MN

**本節**: 4 件（うち検証手順・確認問題 4 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 2677 | MONITOR コマンド (MN) | MN コマンドは TSU LOGON/LOGOFF、JOB 開始/終了、データセット名表示などの監視メッセージ生成を切替える。 マニュアル抜粋 (MN): [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job step; include job name in diagnostic message if job ends abnormally. SESS — display user ID for each TSO/E session when session starts and ends; include user ID in diagnostic if session ends abnormally. -T — display time with job name or user ID (hh:mm:ss); activated for all consoles with MONITOR turned on. STATUS — displ... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2678 | MN JOBNAMES | 全ジョブの開始・終了時にメッセージを生成。ジョブ流れの自動化前提として活用。 マニュアル抜粋 (MN): [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job step; include job name in diagnostic message if job ends abnormally. SESS — display user ID for each TSO/E session when session starts and ends; include user ID in diagnostic if session ends abnormally. -T — display time with job name or user ID (hh:mm:ss); activated for all consoles with MONITOR turned on. STATUS — displ... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2679 | MN STATUS | DD 文割当のたびに DSN を SYSLOG に記録する形式。データセット流出調査などで一時的に有効化。 マニュアル抜粋 (MN): [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job step; include job name in diagnostic message if job ends abnormally. SESS — display user ID for each TSO/E session when session starts and ends; include user ID in diagnostic if session ends abnormally. -T — display time with job name or user ID (hh:mm:ss); activated for all consoles with MONITOR turned on. STATUS — displ... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2680 | MN SESS | TSO LOGON/LOGOFF のたびにメッセージを出す。セキュリティ監査補助。 マニュアル抜粋 (MN): [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job step; include job name in diagnostic message if job ends abnormally. SESS — display user ID for each TSO/E session when session starts and ends; include user ID in diagnostic if session ends abnormally. -T — display time with job name or user ID (hh:mm:ss); activated for all consoles with MONITOR turned on. STATUS — displ... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#2677 MONITOR コマンド (MN)"

    **検証手順**

    検証目的: 「MONITOR コマンド (MN)」(MN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] MN を投入 (運用目的: MONITOR コマンド (MN))
    [オプション説明]
      MN - MONITOR コマンド (MN) を確認するための MN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job ste...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=MONITOR コマンド (MN) に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「MONITOR コマンド (MN)」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (MN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「MONITOR コマンド (MN)」(MN) の説明として正しいものはどれか?

    - A. 全ジョブの開始・終了時にメッセージを生成。ジョブ流れの自動化前提として活用。
    - B. DD 文割当のたびに DSN を SYSLOG に記録する形式。データセット流出調査などで一時的に有効化。
    - C. MN コマンドは TSU LOGON/LOGOFF、JOB 開始/終了、データセット名表示などの監視メッセージ生成を切替える。
    - D. TSO LOGON/LOGOFF のたびにメッセージを出す。セキュリティ監査補助。

    正解: **C**

    解説: 正解 (C) は「MONITOR コマンド (MN)」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「MN」内の隣接項目要約であり、「MONITOR コマンド (MN)」の説明としては誤り。実機 MN で検証し、出典章で詳細確認推奨。


??? example "#2678 MN JOBNAMES"

    **検証手順**

    検証目的: 「MN JOBNAMES」(MN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] MN を投入 (運用目的: MN JOBNAMES)
    [オプション説明]
      MN - MN JOBNAMES を確認するための MN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job ste...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=MN JOBNAMES に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「MN JOBNAMES」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (MN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「MN JOBNAMES」(MN) の説明として正しいものはどれか?

    - A. MN コマンドは TSU LOGON/LOGOFF、JOB 開始/終了、データセット名表示などの監視メッセージ生成を切替える。
    - B. DD 文割当のたびに DSN を SYSLOG に記録する形式。データセット流出調査などで一時的に有効化。
    - C. 全ジョブの開始・終了時にメッセージを生成。ジョブ流れの自動化前提として活用。
    - D. TSO LOGON/LOGOFF のたびにメッセージを出す。セキュリティ監査補助。

    正解: **C**

    解説: 正解 (C) は「MN JOBNAMES」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「MN」内の隣接項目要約であり、「MN JOBNAMES」の説明としては誤り。実機 MN で検証し、出典章で詳細確認推奨。


??? example "#2679 MN STATUS"

    **検証手順**

    検証目的: 「MN STATUS」(MN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] MN を投入 (運用目的: MN STATUS)
    [オプション説明]
      MN - MN STATUS を確認するための MN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job ste...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=MN STATUS に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「MN STATUS」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (MN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「MN STATUS」(MN) の説明として正しいものはどれか?

    - A. MN コマンドは TSU LOGON/LOGOFF、JOB 開始/終了、データセット名表示などの監視メッセージ生成を切替える。
    - B. 全ジョブの開始・終了時にメッセージを生成。ジョブ流れの自動化前提として活用。
    - C. DD 文割当のたびに DSN を SYSLOG に記録する形式。データセット流出調査などで一時的に有効化。
    - D. TSO LOGON/LOGOFF のたびにメッセージを出す。セキュリティ監査補助。

    正解: **C**

    解説: 正解 (C) は「MN STATUS」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「MN」内の隣接項目要約であり、「MN STATUS」の説明としては誤り。実機 MN で検証し、出典章で詳細確認推奨。


??? example "#2680 MN SESS"

    **検証手順**

    検証目的: 「MN SESS」(MN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] MN を投入 (運用目的: MN SESS)
    [オプション説明]
      MN - MN SESS を確認するための MN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieae200] MONITOR optional keyword — system reports on selected events. Specify one or more parameters separated by blank or comma: MONITOR(JOBNAMES-T,SESS-T,STATUS). JOBNAMES — display names of jobs when they start and end; display I/O device allocation at start of job ste...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=MN SESS に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「MN SESS」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (MN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: 「MN SESS」(MN) の説明として正しいものはどれか?

    - A. MN コマンドは TSU LOGON/LOGOFF、JOB 開始/終了、データセット名表示などの監視メッセージ生成を切替える。
    - B. 全ジョブの開始・終了時にメッセージを生成。ジョブ流れの自動化前提として活用。
    - C. TSO LOGON/LOGOFF のたびにメッセージを出す。セキュリティ監査補助。
    - D. DD 文割当のたびに DSN を SYSLOG に記録する形式。データセット流出調査などで一時的に有効化。

    正解: **C**

    解説: 正解 (C) は「MN SESS」の単一焦点要約 (出典: z/OS MVS System Commands、v3 抽出時 一次資料 由来)。他選択肢は同じ Lv2「MN」内の隣接項目要約であり、「MN SESS」の説明としては誤り。実機 MN で検証し、出典章で詳細確認推奨。


## STOPMN

**本節**: 2 件（うち検証手順・確認問題 2 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 2681 | STOPMN JOBNAMES | MN JOBNAMES で開始した監視を停止する。 マニュアル抜粋 (STOPMN): [zOS31_ieag100] STOPMN command — STOPMN\|PM with parameters: JOBNAMES (stop jobname display specified in MONITOR JOBNAMES), DSNAME (stop display of non-temporary data set names specified in MONITOR DSNAME), SPACE (stop display of available space on direct access volumes specified in MONITOR SPACE), STATUS (stop display of data set names, volume serial numbers, status specified in MONITOR STATUS), SESS (stop display of time-sharing user identifiers). L=name console name where system presents display (default: console where STOPMN entered). Example: PM JOBNAMES discontinues display of job name in... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |
| 2682 | STOPMN STATUS | MN STATUS を停止する。継続使用は SYSLOG 肥大化のため必要に応じてオフにする。 マニュアル抜粋 (STOPMN): [zOS31_ieag100] STOPMN command — STOPMN\|PM with parameters: JOBNAMES (stop jobname display specified in MONITOR JOBNAMES), DSNAME (stop display of non-temporary data set names specified in MONITOR DSNAME), SPACE (stop display of available space on direct access volumes specified in MONITOR SPACE), STATUS (stop display of data set names, volume serial numbers, status specified in MONITOR STATUS), SESS (stop display of time-sharing user identifiers). L=name console name where system presents display (default: console where STOPMN entered). Example: PM JOBNAMES discontinues display of job name in... (出典: z/OS MVS System Commands、原文ママ) | AS1, ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#2681 STOPMN JOBNAMES"

    **検証手順**

    検証目的: 「STOPMN JOBNAMES」(STOPMN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] STOPMN を投入 (運用目的: STOPMN JOBNAMES)
    [オプション説明]
      STOPMN - STOPMN JOBNAMES を確認するための STOPMN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag100] STOPMN command — STOPMN|PM with parameters: JOBNAMES (stop jobname display specified in MONITOR JOBNAMES), DSNAME (stop display of non-temporary data set names specified in MONITOR DSNAME), SPACE (stop display of available space on direct access volumes specified ...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=STOPMN JOBNAMES に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「STOPMN JOBNAMES」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (STOPMN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: (この項目の確認問題は一次資料との突合後に追加予定)


    正解: **-**

    解説: [Quiz 生成不可]


??? example "#2682 STOPMN STATUS"

    **検証手順**

    検証目的: 「STOPMN STATUS」(STOPMN) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] STOPMN を投入 (運用目的: STOPMN STATUS)
    [オプション説明]
      STOPMN - STOPMN STATUS を確認するための STOPMN 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      [システム応答メッセージID] ... (一次資料 抜粋: [zOS31_ieag100] STOPMN command — STOPMN|PM with parameters: JOBNAMES (stop jobname display specified in MONITOR JOBNAMES), DSNAME (stop display of non-temporary data set names specified in MONITOR DSNAME), SPACE (stop display of available space on direct access volumes specified ...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=STOPMN STATUS に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「STOPMN STATUS」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (STOPMN を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: (この項目の確認問題は一次資料との突合後に追加予定)


    正解: **-**

    解説: [Quiz 生成不可]


## JES2

**本節**: 3 件（うち検証手順・確認問題 3 件）

| 連番 | 技術項目 | 内容説明 | 出典 |
|---:|---|---|---|
| 2683 | $D A 活動状況 | JES2 イニシエータの活動状況、各クラスの稼動ジョブ一覧を表示する JES2 コマンド (MVS コマンドではなく JES2 サブコマンド)。 マニュアル抜粋 (JES2): [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within command; JES2 removes all blanks during processing. $P command authority requires system authority and control security access authority. $P scope is single member; for MAS use MVS ROUTE *ALL. $DMEMBER indicates if $P is in effect. $S CNVT — allows JES2 converters to select work and restart conversion processing; conv... (出典: z/OS MVS System Commands、原文ママ) | AS2, ZOS31 |
| 2684 | $D Q キュー状況 | JES2 内部の入力・実行待ち・出力待ちキュー件数を表示する。 マニュアル抜粋 (JES2): [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within command; JES2 removes all blanks during processing. $P command authority requires system authority and control security access authority. $P scope is single member; for MAS use MVS ROUTE *ALL. $DMEMBER indicates if $P is in effect. $S CNVT — allows JES2 converters to select work and restart conversion processing; conv... (出典: z/OS MVS System Commands、原文ママ) | AS2, ZOS31 |
| 2685 | $P JES2 ドレイン | JES2 の新規受付を停止しドレインさせる。Z EOD 前に投入する典型シーケンス。 マニュアル抜粋 (JES2): [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within command; JES2 removes all blanks during processing. $P command authority requires system authority and control security access authority. $P scope is single member; for MAS use MVS ROUTE *ALL. $DMEMBER indicates if $P is in effect. $S CNVT — allows JES2 converters to select work and restart conversion processing; conv... (出典: z/OS MVS System Commands、原文ママ) | AS2, ZOS31 |

### 検証手順 ・ 理解度チェック

??? example "#2683 $D A 活動状況"

    **検証手順**

    検証目的: 「$D A 活動状況」(JES2) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] JES2 を投入 (運用目的: $D A 活動状況)
    [オプション説明]
      JES2 - $D A 活動状況 を確認するための JES2 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      HASP000 ... (一次資料 抜粋: [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=$D A 活動状況 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「$D A 活動状況」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (JES2 を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: (この項目の確認問題は一次資料との突合後に追加予定)


    正解: **-**

    解説: [Quiz 生成不可]


??? example "#2684 $D Q キュー状況"

    **検証手順**

    検証目的: 「$D Q キュー状況」(JES2) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] JES2 を投入 (運用目的: $D Q キュー状況)
    [オプション説明]
      JES2 - $D Q キュー状況 を確認するための JES2 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      HASP000 ... (一次資料 抜粋: [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=$D Q キュー状況 に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「$D Q キュー状況」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (JES2 を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: (この項目の確認問題は一次資料との突合後に追加予定)


    正解: **-**

    解説: [Quiz 生成不可]


??? example "#2685 $P JES2 ドレイン"

    **検証手順**

    検証目的: 「$P JES2 ドレイン」(JES2) の構文・パラメータ・運用上の役割を z/OS MVS System Commands マニュアルと実機で確認し、MVS オペレータが正しくコマンド投入・状態判断できることを検証する。

    ```text
    [セッション環境]
    z/OS マスター MCS コンソール (master authority)

    [コマンド 1] JES2 を投入 (運用目的: $P JES2 ドレイン)
    [オプション説明]
      JES2 - $P JES2 ドレイン を確認するための JES2 系コマンド
      L=name - 表示先コンソール/エリア指定 (省略時は投入コンソール)
      (出典: z/OS MVS System Commands、原文ママ)

    [出力 1]
      HASP000 ... (一次資料 抜粋: [zOS31_hasa200] JES2 command identifier — JES2 commands must begin with character identifying command as one processed by JES2. Default is dollar sign ( $ ); can redefine via CONCHAR= on CONDEF initialization statement. Case: uppercase or lowercase. Blanks allowed anywhere within...)
      (出典: z/OS MVS System Commands、原文ママ)

    [コマンド 2] 結果確認: DISPLAY [対象] で実機状態を再確認
    [出力 2]
      [確認用ステータス出力 - サイトの実機環境に依存]
      (出典: z/OS MVS System Commands、原文ママ)
    ```

    前提条件: オペレータ権限 (MCS console master authority)、対象サブシステム稼働中、出典マニュアル参照可能

    プレースホルダ: [対象項目]=$P JES2 ドレイン に対応する対象、[システム応答メッセージID]=実機で表示される 一次資料 マニュアル準拠のメッセージID、[確認用ステータス出力]=サイトの実機環境に依存する出力 (具体値は実機・サイト標準に従う)

    記録項目:

    - ① 出典マニュアル (z/OS MVS System Commands) の「$P JES2 ドレイン」該当章のページ番号と抜粋
    - ② 実機で投入したコマンド全文 (JES2 を含むパラメータ・オペランド値)
    - ③ システム応答メッセージ ID と本文 (z/OS console / SDSF.LOG 採取)
    - ④ 実行前 / 実行後 の対象状態 (D [対象] の差分)
    - ⑤ 異常時のエラーメッセージ全文と RC (出た場合)
    - ⑥ 実機環境情報 (z/OS リリース / SYSNAME / オペレータ ID / 実行時刻)

    ---

    **理解度チェック**

    問題: (この項目の確認問題は一次資料との突合後に追加予定)


    正解: **-**

    解説: [Quiz 生成不可]
